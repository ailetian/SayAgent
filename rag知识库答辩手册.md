# RAG 知识库设计答辩手册（面试可用 · 大白话版）

> 用途：把 hify 知识库的每一个技术选择讲到"小白能听懂、面试官问不倒"的程度。
> 结构：每个技术点 = ①大白话是什么 ②为什么这么选（能自洽） ③面试追问怎么答。
> 适用场景：内部 20–50 人、单租户自托管、文档量 <1 万、准确度优先。

---

## 0. 30 秒电梯陈述（面试开场就说这段）

> "我做的是一个企业内部知识库问答系统。核心链路分两半：**建库**是把文档剪成一张张'语义完整的卡片'，用 BGE-M3 模型把每张卡片变成向量存进 pgvector，同时留一份全文索引；**提问**时用'语义 + 关键词'两条腿一起找卡片，用 RRF 按名次合并，设一条及格线——找不到够格的卡片就老实回答'查不到'，绝不让模型瞎编。每条回答都带出处、每次检索都记日志，所以答案可追溯、答错了能倒查原因。技术选型上我的第一原则是**准确度和可控性优先**，而不是堆最炫的技术。"

这段话覆盖了：切片、向量模型、混合检索、RRF、阈值拒答、溯源、日志、设计哲学。面试官任何一个词往下问，你都能展开。

---

## 1. 为什么选 BGE-M3？（Embedding 模型选型）

### 1.1 大白话：这个模型是干嘛的

它是一台"翻译机"：把一段中文文字，翻译成一串 1024 个数字（叫向量）。**意思相近的文字，翻出来的数字就相近**。以后判断"两段话是不是在说同一件事"，不用读文字，直接算这两串数字的距离就行——机器算数字比读文字快得多。

### 1.2 为什么是它，不是别的（这是核心，能自洽的关键）

我选 BGE-M3 是四个理由叠加，每一条都能对上我的场景：

**① 中文效果处在开源第一梯队。**
我的文档是中文规章/FAQ/技术文档，模型的中文语义理解直接决定"换个说法能不能找到"。BGE 系列是智源（BAAI）专门针对中文优化的，在中文检索评测里长期名列前茅。选它 = 选中文场景里"翻译得最准"的那台机器。

**② 它天生支持三种检索方式，正好戳中我要的"混合检索"。**
BGE-M3 的"M3"是三个 Multi：
- Multi-Functionality（多功能）：一个模型同时能产出**稠密向量**（管"意思像不像"）和**稀疏向量**（管"关键词对不对"）。
- Multi-Linguality（多语言）：100+ 语言，以后有英文文档也不用换模型。
- Multi-Granularity（多粒度）：单次最长能吃 8192 个 token，长段落也能整块编码。

这一条是我选它**最硬的技术理由**：别的 embedding 模型只会"意思像不像"，而我要的恰恰是"意思 + 关键词"两条腿走路，BGE-M3 一个模型就把这两种能力都给了，天然契合我的混合检索架构。

**③ 开源、可本地部署、数据不出内网。**
我的定位是内部自托管，敏感文档不能出网。BGE-M3 开源免费、能跑在本地（我已经把 `models--BAAI--bge-m3` 装在本机），完全满足"数据不出内网 + 不花 API 费"。

**④ 1024 维是"够用又不浪费"的甜点。**
维度越高，语义表达越细，但存储和计算越贵。1024 维在"效果"和"成本"之间是公认的平衡点——比 384 维表达力强得多，又不像 4096 维那样占空间拖慢检索。对我几千到上万张卡片的量级，1024 维绰绰有余。

### 1.3 一个诚实的工程取舍（面试加分点）

BGE-M3 理论上"稠密 + 稀疏"都能出，一个模型就能做完整混合检索。但**我实际的关键词那一路，用的是 PostgreSQL 自带的全文检索（配 zhparser 中文分词），而不是 BGE-M3 的稀疏向量**。

为什么？因为我的技术栈是 **Java + Spring Boot + pgvector**。在这个栈里：
- 稠密向量：pgvector 存 BGE-M3 的 1024 维向量，成熟稳定。
- 关键词：PG 自带的全文检索开箱即用，不用我再搭一套稀疏向量的存储和检索逻辑。

**结论：我用 BGE-M3 的稠密向量 + PG 全文检索组合成混合检索，而不是硬上 BGE-M3 的稀疏向量。** 这是"用最少的工程复杂度拿到混合检索收益"的务实选择。等规模大了、PG 全文不够用了，再切换到 BGE-M3 稀疏向量或专用向量库——架构上留了口子。

> **可能被追问："为什么不直接用 BGE-M3 的稀疏向量做关键词那一路？"**
> 答："能做，但在 Java+pgvector 栈里，PG 全文检索是现成的、零额外依赖，而用稀疏向量我得自己维护一套稀疏索引的存储和打分逻辑。小团队场景，PG 全文的效果完全够，没必要为了'用满一个模型'增加工程复杂度。这是成本收益权衡，不是技术上做不到。"

> **可能被追问："Embedding 模型以后能换吗？"**
> 答："LLM 能随便换，但 **Embedding 不能随便换**。因为向量维度和语义空间是绑死的——换了 embedding，全库几万个向量的'坐标系'就变了，必须全部重新算一遍、重建索引。所以 embedding（BGE-M3 / 1024 维）我第一天就定死，pgvector 的向量列维度严格写 1024。这是我架构里唯一'不可逆'的决定，必须最早锁定。"

---

## 2. 切片规则与原因（为什么这么剪卡片）

### 2.1 大白话：切片是干嘛的

把一篇长文档，剪成一张张"卡片"，每张卡片讲清楚**一件完整的事**。以后 AI 回答问题，拿到的原材料就是这些卡片。**卡片剪得好不好，直接决定答得准不准**——卡片剪坏了，后面检索再强、模型再聪明都救不回来。

### 2.2 第一性原理：为什么必须切，不能整篇丢进去

三个硬约束逼出来的：

1. **整篇转一个向量 = 语义被稀释。** 一篇员工手册里既有年假又有报销又有考勤，全糊成一个向量，问"年假几天"它和问"怎么报销"的相似度差不多，等于没法区分——**主题混在一起，向量就废了**。
2. **LLM 上下文有限。** 不可能把整本手册塞给模型，只能挑最相关的几张卡片喂进去。
3. **要能溯源。** 我要求"有据可查"，答案得定位到具体段落。整篇丢给用户，不叫溯源。

### 2.3 核心原则：切分的理想边界 = 内容天然的语义边界

这是切片能自洽的**总纲**。不同文档"天然的缝"长得不一样，所以按类型区别对待：

| 文档类型 | 怎么切 | 为什么这么切（理由链） |
|---|---|---|
| **FAQ / 问答** | 一问一答 = 一张卡 | 一对 QA 就是"最小完整语义单元"。从中间切开，检索到"半个答案"必然答非所问 |
| **规章制度** | 按"第 X 条"切 | 用户问的就是某一条（"婚假几天"）。按条切 = 检索直接命中完整条款，永远不会截断半句 |
| **Markdown** | 按标题层级（H1/H2/H3）切 | 标题是作者**亲手画好的语义边界**，比任何算法猜段落都准。这也是为什么 MD 必须原生解析、**不能走 Tika**——Tika 会把标题结构拍平，等于自废武功 |
| **PDF / DOCX 普通长文** | 递归切 300–800 字，块间留 10–15% 重叠 | 拿不到可靠结构时的兜底。**重叠的理由**：关键句恰好落在两块交界处时，重叠保证它至少在其中一块里完整出现，不会被切断 |

### 2.4 块该多大？唯一的准绳

- **太小** → 断章取义。"提前 30 日"单独切出来，不知道说的是离职、转岗还是退租。
- **太大** → 一块混多个主题，向量又被稀释，回到"整篇"的老问题。
- **唯一准绳：一块内容能独立回答一个问题。** 这就是为什么我**禁止把切块大小写死成 500/50**——不同文档的"一个完整问题"需要的篇幅不一样，写死必然有一类文档答不准。参数要可配置，按文档类型自适应。

> **可能被追问："你怎么定 300–800 这个范围？"**
> 答："这不是拍脑袋，是两头夹出来的。下限保证一块能装下一个完整语义（太短会断章取义）；上限受两个约束——一是别让单块混太多主题稀释向量，二是要给检索留出'一次能塞几块进 LLM'的空间。300–800 是经验甜点区，而且是**可配置的默认值**，不是硬编码。规章、FAQ 这种有结构的我根本不用这个范围，直接按条/按问答切。"

> **可能被追问："重叠为什么是 10–15%，不是 0 也不是 50%？"**
> 答："0 重叠的风险是关键句正好被切在两块交界处，两块都只有半句。重叠就是给交界处上保险。但重叠太多（比如 50%）会产生大量近乎重复的卡片，既浪费存储又污染检索结果。10–15% 是'保住交界句'和'不制造重复'之间的平衡。"

---

## 3. 检索规则与原因（为什么这么找卡片）

### 3.1 大白话：两个伙计一起找

想象知识库有两个找卡片的伙计：

- **伙计 A（认字的）= 关键词检索（PG 全文 + zhparser）。** 眼睛尖但死板。你说"KPI-2026 考核表"，他一个字一个字对，秒找到。但你问"离职要提前几天"，卡片写的是"辞职须提前 30 日"——字对不上，他两手一摊说没有。**认字不认意思。**
- **伙计 B（懂意思的）= 向量检索（BGE-M3 + pgvector）。** 聪明，知道"离职 = 辞职"，换着法问都难不倒他。但你要"KPI-2026"这种精确编号，他觉得所有绩效文件"意思都挺像"，抱一堆回来让你自己挑。**懂意思不认字。**

### 3.2 为什么必须两个都要（能自洽的核心）

**不是"两个更高级"，是"缺谁都有一类问题必挂"：**
- 只用向量（伙计 B）→ 精确编号、数字、专有名词找不准（语义检索天生对"字面精确"迟钝）。
- 只用关键词（伙计 A）→ 换个说法就查不到（关键词天生对"同义改写"迟钝）。
- 两者的短板**恰好互补**，所以必须并行。这是**混合检索**的全部理由。

### 3.3 两份名单怎么合并？——RRF（按名次投票）

两个伙计各交一份排好序的名单，但**打分标准不一样**：向量给的是余弦相似度（0–1），关键词给的是全文得分（没有上界）。**分数单位不同，没法直接比大小。**

解决办法：不比分数，**只比名次**。你（向量）排第 1、我（关键词）排第 2 的卡片，肯定比"你排第 8、我排第 9"的靠谱。两边都排前面的卡片最终胜出。这就是 **RRF（Reciprocal Rank Fusion，倒数排名融合）**——本质就是"看名次投票"。

> RRF 公式（面试可写）：每张卡片的最终分 = Σ 1/(k + 该路排名)，k 是常数（常用 60）。排名越靠前，1/(k+rank) 越大。两路都靠前的卡片，两个分数加起来最高。

### 3.4 为什么要"及格线 + 拒答"

两个伙计找了半天，最好的卡片也就"三分像"，这时**硬塞给 AI，它一定会看着三分像的东西编出十分像的答案**——这是 RAG 最危险的翻车方式（幻觉）。

所以设一条及格线（相似度阈值）：**最好的卡片都不及格，就直接回"知识库里查不到"，绝不喂给 LLM 硬编。** 这条兜底直接支撑我"准确度优先"的定位——**宁可不答，不许瞎编**。

### 3.5 两个配套增强（低成本高收益）

- **Query Rewriting（问题改写）：** 把口语问题改成标准问法，多轮对话里补全指代。例：上一句问"年假几天"，这句问"那婚假呢？"——改写成"婚假几天"再去检索。首版做轻量版即可。
- **Small-to-Big（先小后大）：** 用小卡片精确命中，但喂给 LLM 时把命中卡片的**前后相邻块**也带上，避免断章取义。成本极低（只多取相邻块），建议随混合检索一起做。

### 3.6 溯源 + 日志（贯穿全程的两条铁律）

- **每条回答附来源：** 答案后面标"来自 XX 文档第 X 条 / 第 X 段"。用户能核对，审查能追责，这是"可信"的前提。
- **检索日志记账：** 每次问答记下"问了啥、翻了哪几张卡、各自多少分、最后用了哪几张"。哪天答错了，翻日志一看就知道错在哪环——是**卡片剪坏了**（内容缺胳膊少腿）还是**没找对**（对的卡片没被翻出来），照着修。这就是"方便后续追踪优化"的落点。

> **可能被追问："阈值定多少？"**
> 答："阈值不能拍脑袋写死，要用评测集调出来。做法是：拿一批'标准问答'和一批'库里根本没有的问题'，跑一遍看分数分布，找到一个'能答的基本都过线、该拒的基本都被拦下'的临界点。而且这个阈值应该做成可配置，不同库可以微调。"

---

## 4. 程序写好后的判断标准（怎么验收"准不准"）

这一节回答你最关心的："程序写完了，我怎么知道它到底行不行？"

### 4.1 两种检验方式（都做）

**① 试问台（日常快速验，小白友好）**
库里输入一个问题，当场看三样东西：**检索到哪几张卡片、AI 怎么答的、来源是哪张卡**。最直观，随时能验，用来日常发现问题。

**② 固定题集打分（上线前正式体检，客观）**
预先准备几十条"标准问答"（问题 + 期望答案 + 期望命中的文档），让系统自动跑一遍打分。更客观，是上线前的"体检门禁"——不达标不上线。

### 4.2 四个核心指标（每个库的"体检页"）

| 指标 | 大白话 | 内部场景达标线 | 反映什么 |
|---|---|---|---|
| **命中率（召回率@5）** | 该找到的内容，前 5 张卡片里有没有 | ≥ 90% | 找得全不全 |
| **忠实度（Faithfulness）** | AI 的回答是不是真的基于卡片，有没有编 | ≥ 85% | 靠不靠谱、有没有幻觉 |
| **拒答率** | 库里没有的问题，AI 有没有老实说"查不到" | ≤ 5%（该答的却拒答的比例） | 覆盖够不够 / 会不会误拒 |
| **响应速度（P95）** | 95% 的问题多久能答完 | ≤ 3 秒 | 快不快 |

> 这四个指标里，**命中率和忠实度是命门**（决定"准"），速度是体验，拒答率是覆盖度的照妖镜。基础健康度（文档数/切块数/索引成功数）作为最底层的"设备是否正常"也要显示。

### 4.3 为什么用这套标准，而不是更复杂的

我的场景是内部业务系统、准确度优先。这套标准（业务系统档）在"够严"和"不过度"之间平衡：
- 不追求学术级的 RAGAS 全套指标（那是做平台产品才需要）。
- 但**命中率和忠实度这两条命门必须卡死**，因为 Agent 基于知识库回答，答错有业务风险。
- **关键澄清**：AGENTS.md 里砍掉的是"评测台产品"（一套可视化平台系统），而我这里要的是"评测集 + 门禁动作"（几十条用例 + 上线前打勾）——这是质量保障动作，不做成产品，两者不矛盾。

> **可能被追问："没有标注数据怎么建评测集？"**
> 答："冷启动就用最小集：挑 3–5 篇代表性文档，每篇人工写 1–2 个问题和期望答案，先有 10–20 条基线用例把门禁跑起来。上线后再从**真实检索日志**里捞高频问题和答错的 case，持续往评测集里加。评测集是养出来的，不是一次做完的。"

---

## 5. 扩大规模 / 过程中的坑，怎么调整（前瞻性 = 面试高分区）

这一节回答："以后规模大了，或者中途踩坑，你怎么办？"——能答好这段，面试官会觉得你真的想清楚了。

### 5.1 十个已知坑 + 对应调整

| # | 坑 | 症状 | 怎么调整 |
|---|---|---|---|
| 1 | **中文没分词就上全文检索** | 关键词那一路几乎全废 | 必须配 zhparser（PG 中文分词插件），这是中文 FTS 的前提，不是可选项 |
| 2 | **切块大小写死 500/50** | 有的文档答不准 | 参数可配 + 按类型自适应（见第 2 节） |
| 3 | **重复导入产生重复向量** | 同一答案反复出现在结果里，挤掉别的 | 用文件 checksum 去重，同一文件重复上传直接跳过或覆盖 |
| 4 | **换了 embedding 模型没重建向量** | 检索突然全乱 | 维度和模型第一天定死；真要换 = 全库重建，走离线迁移 |
| 5 | **把 TOP_K 当阈值用** | 每次都强行返回 K 条，哪怕全不相关 | TOP_K（取几条）和阈值（够不够格）是两件事，必须分开 |
| 6 | **Markdown 走了 Tika** | 标题结构被拍平，切块变差 | MD 原生解析，保住标题层级 |
| 7 | **同步解析大文件** | 传个大 PDF 把线程卡死 | 解析/向量化走异步任务队列，前端轮询进度 |
| 8 | **未提交的上传变孤儿文件** | 存储悄悄泄漏 | 草稿态 + 24h TTL 清理任务 |
| 9 | **检索不记日志** | 答错了无从查起 | 每次检索落日志（问题/命中/分数），这是优化的唯一抓手 |
| 10 | **不给来源** | 用户不敢信，也没法审查 | 每条回答强制附来源片段定位 |

### 5.2 规模变大时的演进路线（架构留的口子）

我的设计**现在够用、将来能长**，不同规模对应不同动作：

| 触发条件 | 加什么 | 为什么现在不加 |
|---|---|---|
| **文档量 > 1 万，精确率不达标** | 加 **Reranker（精排）** | 现在文档少，加了只增延迟（+100–500ms）和成本，收益不抵成本 |
| **召回率持续不达标** | 加 **Multi-Query（多路召回）** | 现在 Query Rewriting + 混合检索已覆盖大部分，多路召回要多次 LLM 调用，延迟高 |
| **并发变高、热点问题多** | 加**热点答案缓存** | 内部 20–50 人并发低，缓存收益小 |
| **pgvector 撑不住（百万级向量）** | 换专用向量库（Milvus/Qdrant） | pgvector 在万级完全够，且省一套独立组件的运维 |
| **PG 全文不够用** | 切到 BGE-M3 稀疏向量 / ES | 现在 PG 全文够用，换了增加复杂度 |
| **要流式打字机效果** | LLM 接口改流式返回 | 属 M6 聊天 UI 体验，不阻塞知识库引擎 |

> **这套演进逻辑的自洽内核：** 每一个"暂时不加"都有明确的**触发条件**——不是"做不了"，是"现在加不划算"。等真实痛点出现（延迟高了、召回差了、库爆了）再加，用数据说话，而不是提前过度设计。这就是"可运维、灵活"落到架构上的样子。

> **可能被追问："Reranker 到底是干嘛的，为什么它能提精确率？"**
> 答："混合检索是'粗筛'，快但不够精。Reranker 是'精挑'——把粗筛出的前 20 张卡片，用一个更重的模型逐一和问题做精细比对，重新排序取前 5。它牺牲一点速度换精确率。文档量小的时候粗筛就够准，没必要多这一道；文档量大、相似内容多、粗筛区分不开时，Reranker 的价值才体现出来。"

> **可能被追问："为什么不一开始就用 Milvus 这种专业向量库？"**
> 答："因为 pgvector 让我把向量和业务数据放在同一个 PostgreSQL 里，一套数据库搞定，事务、备份、权限全统一，运维成本最低。专业向量库是另一套要独立部署和运维的系统，只有当向量到百万级、pgvector 性能撑不住时才值得。小团队场景，少一个组件就少一份运维负担。"

---

## 6. 一页纸速记（面试前扫一眼）

```
定位：内部知识库问答，准确度 + 可控性优先，自托管不出网

模型：BGE-M3（1024维）
  为什么：中文强 / 三合一(稠密+稀疏+多语言，契合混合检索) / 开源本地 / 1024维是甜点
  铁律：embedding 不能随便换（换=全库重建），第一天定死维度

切片：边界 = 内容天然语义边界
  FAQ=一问一答 / 规章=按条 / MD=按标题(不走Tika) / 长文=300-800字+10-15%重叠
  准绳：一块能独立回答一个问题；禁写死 500/50

检索：两条腿走路（缺一路必有一类问题挂）
  关键词(PG全文+zhparser，认字) + 向量(BGE-M3，懂意思)
  RRF 按名次合并（分数单位不同，只能比名次）
  阈值+拒答（不及格就说查不到，绝不瞎编）
  配套：Query改写 + Small-to-Big

验收：试问台(日常) + 题集打分(上线门禁)
  四指标：命中率@5≥90% / 忠实度≥85% / 拒答率≤5% / P95≤3s

溯源+日志：每条回答带来源，每次检索记账（答错能倒查）

演进：每个"暂不加"都有触发条件
  >1万文档→Reranker / 召回差→多路召回 / 并发高→缓存 / 百万向量→Milvus
```

---

## 附：最容易被问倒的 3 个"陷阱题"

1. **"你这不就是个搜索引擎套个 GPT 吗？"**
   → "区别在三点：一是切片让每块知识语义完整，搜索引擎不管这个；二是混合检索+RRF 解决了'换说法找不到'和'精确编号找不准'的互补短板；三是阈值拒答 + 溯源，保证它'找不到就不装懂、答了必有据'——这恰恰是把 LLM 的幻觉关进笼子的部分，纯搜索引擎 + GPT 做不到。"

2. **"向量检索不是已经很强了，为什么还要关键词？"**
   → "向量对'字面精确'天生迟钝。问'KPI-2026 考核表'，向量觉得所有绩效文件都差不多，反而找不准那个精确编号。关键词检索专治这个。两者短板互补，缺关键词那一路，一整类'精确编号/数字/专有名词'的问题就会挂。"

3. **"如果模型答错了，你怎么定位是哪出的问题？"**
   → "翻检索日志。日志记了'问了啥、翻了哪几张卡、各自分数'。如果对的卡片压根没被找出来，是**检索问题**（可能切片坏了或阈值太严）；如果卡片找对了但答偏了，是**生成问题或卡片内容本身不全**。分层定位，照着修。没有日志就只能靠猜，这就是我把日志当铁律的原因。"
