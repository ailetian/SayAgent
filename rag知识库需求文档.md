# RAG 知识库需求文档（完整可落地版 v2）

> **一句话**：本文是 hify「M5 知识库 + RAG」的**唯一需求基准**，整合了多轮讨论已定稿的全部决策，做到"先定需求、再开发"，避免"能检索但答不准、不可验收"的盲目开发。
>
> **与其他文档关系**：
>
> - `plans/06_M5_知识库与RAG.md` = 任务拆分（T1~T6 怎么做）；
> - `rag知识库答辩手册.md` = 面试/答辩用的大白话讲解（为什么这么设计）；
> - **本文 = 要做成什么样、按什么标准验收（做什么 + 验收）**。
> - 经验来源：项目根 `rag-knowledge/`（设计模式 / 错误模式 / 评测标准 / 上线清单 / 高频规则 / 代码片段），已逐条翻译到 hify 的 Java + pgvector 栈。

---

## 0. 阅读指引

| 你是谁         | 直接看                               |
| ----------- | --------------------------------- |
| 想快速了解全貌     | §1 场景约束 + §2 心智模型                 |
| 前端开发        | §3 产品需求 + §4 上传规格                 |
| 后端 / RAG 开发 | §5 引擎设计 + §6 模型 + §7 参数 + §8 数据模型 |
| 测试 / 验收     | §9 验收标准                           |
| 面试 / 答辩     | §5 理由链 + §13 速记                   |

**技术栈（已确认）**：Spring Boot + Vue3 + MySQL（业务库）+ **PostgreSQL + pgvector**（向量库）+ Redis + **LangChain4j**。

---

## 1. 场景约束（一切取舍的唯一基准）

来自 `AGENTS.md` + 用户多轮确认，硬约束共 5 条：

1. **规模小可控**：内部 20~50 人、单租户、自托管 → 不做多租户/计费/分布式。
2. **单人全栈开发、求稳**：AI 生成样板 + 人审组装 → 功能"够用不出错"，拒绝堆复杂度。
3. **文档量中等、更新不频繁**：规章/FAQ/技术文档为主，月度/季度更新，总量远 < 1 万篇。
4. **准确度优先**：Agent 基于知识库回答，**错答有业务风险** → 宁可拒答不许瞎编。
5. **可运维、灵活**：库/文档/挂载可随时增删改，状态可见，失败可重试，每库可独立体检。

> 这 5 条直接决定：混合检索 + 阈值拒答 + 溯源是**地基**；Rerank / 多路召回 / 缓存 / 流式 / 多模态 / 文档级权限 / 跨库全局搜索都是**二期或不做**。



---

## 2. 心智模型（大白话，先建立全局观）

整个系统分两个阶段：

**建库阶段（后台默默干，用户只管传文档）**

```
上传文档 → 解析出纯文本 → 剪成"卡片"(切片) → 每张卡片转成一串数字(向量) → 存进向量库 → 索引就绪
```

> 一份文档不能整篇转一个向量（主题全糊在一起=语义稀释），必须先剪成"一张卡片讲清一件事"的小块。

**提问阶段（有人用挂了该库的 Agent 提问时）**

```
提问 → (可选)口语改写 → 两个"伙计"一起找卡片(关键词+语义) → 按名次合并(RRF) → 够不够格?
   ├ 够格 → 补齐上下文 → 交给 LLM 照着卡片作答 → 附上来源
   └ 不够格 → 老实回"知识库里查不到"(拒答，绝不瞎编)
每次问答都记账(检索日志)，答错了能顺着账本查是"卡片剪坏"还是"没找对"
```

**Agent 与知识库的关系（核心新需求）**：**多对多挂载**。一个 Agent 可挂多个库，一个库可被多个 Agent 共享；**没挂载连线 = 用这个 Agent 的人完全查不到那个库**（隔离）。

---

## 3. 产品需求

### 3.1 信息架构（两级导航）

```
知识库（主导航）
 └─ 知识库列表页            ← 默认落地页，只显示当前用户有权限的库
      ├─ 点击某卡片 → 知识库详情页（只显示该库的内容）
      └─ ＋创建知识库 → 创建流程 → 成功后进该库详情页
```

**铁律**：列表是容器，详情是内容；详情页永远按 `kb_id` 过滤，绝不串库（由 `Document.kbId` 保证）。从详情页返回始终回列表页，面包屑稳定。

### 3.2 知识库列表页（落地页）

- 顶部：搜索框 + 分类 Tab（我创建的 / 全部·仅管理员可见）。
- 卡片：`图标 + 名称 + 描述 + 文档数 + 最近更新时间`。
- 右上角常驻「＋创建知识库」。
- 空状态：引导「＋创建知识库」大按钮（创建后即可上传文档）。
- 库多时分页 / 无限滚动。
- **可见范围（对齐 §3.5 权限模型）**：列表页是"管理入口"，只对**库 owner（创建者）与全局管理员**展示对应库；普通用户不进列表管理库，他们只通过挂载了库的 Agent 间接使用知识。

### 3.3 知识库详情页（默认进「文档」Tab）

三个 Tab：

- **① 文档 Tab**：按 `kb_id` 过滤的文档列表，字段 `名称 / 类型图标 / 大小 / 索引状态(待索引·索引中·已索引·失败) / 更新时间 / 来源`；支持多选批量删除 / 重新索引；点文档 = 预览 + 元信息 + 重新索引（不是下载）。
- **② 问答 Tab（一等公民）**：直接提问 → 回答带**来源引用片段**（可点击定位到具体 chunk）→ 支持多轮；库未索引完成时提示"暂无可用内容"。
- **③ 设置 Tab**：库重命名、切块策略/参数、可被挂载开关（`is_public`，预留）、归档/删除、（可选）高级检索参数。**不再有成员/角色管理**（见 §3.5 权限模型）。

### 3.4 创建知识库（唯一模式：先建库，再上传）

> 定稿（2026-07-27）：**去掉"上传即建库"**。库必须先创建成功，才存在"往里传文档"这件事——建库与传文档是两个明确分开的步骤，简单、不出孤儿文件。

- **创建流程（分步向导 or 弹窗两步）**：
  1. **第一步（必填）**：填名称/描述（可选切块策略，默认 auto）→ 点确认 → **库立即创建成功**（此时已是合法空库）。
  2. **第二步（可选，创建流程内直接接上）**：进入"上传文档"步骤，可当场拖入文件开始索引；也可点"跳过"，之后随时进库详情页再传。
- **好处**：
  - 没有"拖了文件却没建库"的中间态 → **无孤儿文件问题，无需 24h TTL 清理任务**（该需求随之删除）；
  - 库名永远是用户明确填写的，不存在"预填首文件名"的猜测逻辑；
  - 前后端实现都更简单：建库接口与上传接口完全解耦。
- **不做**：拖文件到列表页空白处直接建库（隐藏快捷入口）—— 已随"上传即建库"一并砍掉。

### 3.5 Agent ↔ 知识库挂载 + 权限模型（核心，2026-07-27 定稿）

**权限模型：能力委托（capability delegation），不做库级 RBAC。**

- **查询权（谁能"用"库）**：完全由 Agent 挂载决定。用户能不能查到某库 = 他用的 Agent 有没有挂这个库。**知识库自身不设 viewer/editor 等角色**，现有 `KbAccess` 退休/大幅简化。
- **管理权（谁能"改"库）**：最小化保留——仅 **库 owner（`created_by`）+ 全局管理员** 可增删文档、改参数、重命名、删库、开体检/试问台。这不是角色体系，只是一个字段判断。
- **挂载权（真正的安全闸门）**：仅 **Agent 的创建者 / 管理员** 可配置该 Agent 挂哪些库；普通使用者只能用不能改。防止任何人新建 Agent 随手挂敏感库绕过隔离。
- **多对多挂载**：新增关联表 `agent_kb_link`，Agent 提问时**自动在其挂载的所有库里一起检索**（用户无感，不手选库）。
- **未挂载 = 隔离**：没挂的库对该 Agent 完全不可见、查不到 → 若挂载库里都查不到 → 拒答。
- **预留**：`KnowledgeBase.is_public`（可否被挂载）字段先加、默认放开，未来敏感库可标记"私有"，须库 owner 授权才能被挂载。

**检索鉴权链一句话**：`用户能用这个 Agent → Agent 挂了这些库 → 只在这些库里检索`；没挂 = 查不到 = 拒答。

### 3.6 文档管理（灵活增删改）

- **增**：上传（4 种类型见 §4）/ 粘贴文本（TEXT）/ URL（预留）。
- **删**：单个 / 批量 → 走**回收站软删**（30 天可恢复），非物理删。现有 Entity 已用 `@SQLDelete + deleted=1` 实现软删，回收站 = 查 `deleted=1` 的记录。
- **改**：文档内容变更 = **重传替换**（删旧 chunk + 传新 → 自动重新索引）；基于 `checksum` 去重，内容没变不重复向量化（§5、R8）。

### 3.7 库管理（重命名 / 删除）

- **重命名**：列表卡片 & 设置 Tab 均可；库名允许重名（靠 id 区分），UI 提示"已存在同名库"。
- **删除**：走回收站软删（同 3.6），30 天内可恢复，之后由清理任务物理删（连带 pgvector 里的向量）。
- **归档**：可选，冷库归档不占列表主视图。

### 3.8 库体检页 + 独立检验（用户强调："能独立检验、有清晰基础指标、别太复杂、最主要要准"）

**体检页 3 个指标（一眼看懂）**：

| 指标         | 含义                        | 反映      |
| ---------- | ------------------------- | ------- |
| **基础健康**   | 文档数 / 切块数 / 索引状态（几篇没索引成功） | 库"建好了没" |
| **检索命中质量** | 用测试问题跑一遍，看能不能找对内容（命中率）    | 库"准不准"  |
| **响应速度**   | 平均/95 分位回答耗时              | 库"快不快"  |

> 拒答率用户未选，列为**可选、默认不显示**。

**两种检验方式（都做）**：

- **试问台**（日常当场验）：在库里输入一个问题，当场看：检索到哪几段 + AI 怎么答 + 来源是啥。小白友好。
- **题集打分**（上线前正式体检）：预置几十条标准问答，系统自动跑一遍打分（对齐 §9 指标）。

> 注意：这是**开发自测的质量动作**，不是 `AGENTS.md` 砍掉的"对外评测平台"。两者不矛盾。

### 3.9 后台处理流水线（异步，核心；含批量上传与失败重试——2026-07-27 定稿补强）

**基本链路（每个文档独立走一遍）**：

```
上传(UPLOAD) → 解析(PARSE) → 切分(CHUNK) → 向量化(EMBED) → 写入 pgvector(STORE) → 索引就绪(READY)
                    ↓任一节点失败：该文档状态置 FAILED，记录 fail_stage + error_code + error_message
```

**① 批量上传（必须支持）**

- 一次可选/拖入**多个文件**（单次 ≤ 10 个，单文件 ≤ 20MB，见 §4 护栏）。
- 上传接口收到文件后**立即返回**（只做白名单/大小/Magic Number 校验 + 落盘 + 建 `indexing_job`），不阻塞等待解析向量化——即"上传即受理，处理全异步"。
- **每个文件一条独立的 `indexing_job`**，互不影响：第 3 个文件解析失败，不影响其余 9 个继续索引成功。禁止把一批文件绑成一个大事务。

**② 进度可见（逐文件、逐节点）**

- 前端轮询（首版轮询即可，WebSocket 推送二期）批量任务状态，文档列表逐行展示：
  `排队中 → 解析中 → 切分中 → 向量化中(可带 n/m 块进度) → 已就绪 / 失败`。
- 批量上传后弹出/驻留一个**上传任务面板**：本批 N 个文件各自的实时状态一屏可见。

**③ 失败必须"看得见节点 + 一键重试"（用户明确要求）**

- **失败节点可见**：`indexing_job.fail_stage` 精确标记死在哪一环（UPLOAD/PARSE/CHUNK/EMBED/STORE），UI 直接显示"❌ 解析失败：加密 PDF"而不是笼统的"处理失败"。错误码细分沿用 §4 护栏 3（加密 PDF / 扫描件无文本 / 格式损坏 / embedding 服务不可用 / 超时…）。
- **单文件重试**：失败行旁边就是「重试」按钮，**从失败的节点断点续跑**（如 EMBED 失败重试时不重新解析切分，复用已产出的中间结果；PARSE 失败则整体重跑）。
- **批量重试**：任务面板提供「重试全部失败项」。
- **重试上限**：自动不重试（避免坏文件死循环），手动重试不限次；每次重试 `retry_count+1` 并保留上次错误信息，便于排查。
- **不静默吞**：任何失败都必须落 `error_code + error_message`，且文档留在列表里可见（状态=失败），不允许消失。

**④ 并发与背压**

- 解析/向量化走独立线程池（如各 2~4 线程），队列排队，防止一批 10 个 20MB PDF 同时打爆内存/embedding 服务。
- 同一文件重复提交（checksum 相同且已 READY）直接秒回"已存在"，不重复索引（R8）。

---

## 4. 文档上传规格（首版 4 种）

**首版支持：TXT / Markdown / PDF(文本层) / DOCX。** 判断标准：内部文档 95% 是这四种形态，且解析成本可控。

| 类型           | 解析方式（Java）           | 关键点                                          |
| ------------ | -------------------- | -------------------------------------------- |
| **TXT**      | 直接读                  | 兜底格式                                         |
| **Markdown** | **原生解析，不走 Tika**     | 必须保留标题层级，才能做"按标题结构切块"（R1 前提）；Tika 会拍平结构=自废武功 |
| **PDF(文本层)** | Apache Tika / PDFBox | 规章、正式文档主力                                    |
| **DOCX**     | Apache Tika / POI    | 行政/HR 文档主力                                   |

> 工程事实：**引入 Tika 一个依赖，PDF + DOCX 同时覆盖**，边际成本近零，无需分批。

**暂不做**：扫描版 PDF（无文本层→**检测后直接拒收并提示"请传文本版"**，不硬啃 OCR）、XLSX（表格切块是重灾区，二期）、PPT（信息密度低）、HTML/URL 抓取（字段预留，二期）、图片/多模态（砍掉）。

**三个必落护栏**：

1. **白名单校验**：后端按扩展名 + Magic Number 双重校验（防改后缀绕过），前端 `accept` 仅提示。
2. **大小上限**：单文件 ≤ 20MB、单次批量 ≤ 10 个；**多文件上传是一等能力**（每文件独立异步 job，互不影响，详见 §3.9）。
3. **失败码细分**：「加密 PDF」「扫描件无文本」「格式损坏」是不同失败码，暴露在文档状态里，别统一报"解析失败"。

---

## 5. RAG 引擎设计与理由链（面试可直接讲）

> 本节每条都遵循"是什么 → 为什么这么做（推导，不是选择） → 面试追问怎么答"。

### 5.1 切片（R1）：按文档类型自适应，禁写死 500/50

**第一性原理**：AI 作答的原材料就是"切出来的块"，切错了后面检索再好也救不回。

**为什么必须切**：①整篇转一个向量 = 语义稀释（问"年假"和问"报销"相似度差不多）；②LLM 上下文有限，只能塞最相关几段；③要"有据可查"，来源必须定位到段落。

**为什么按类型区别切**（切分的理想边界 = 内容天然的语义边界）：

| 库类型           | 切法      | chunk_size | overlap | 理由                                  |
| ------------- | ------- | ---------- | ------- | ----------------------------------- |
| FAQ / 短问答     | 一问一答=一块 | 300~500    | 50~80   | 从中间切=检索到"半个答案"                      |
| 规章制度          | 按"第X条"切 | 800~1000   | 100~150 | 用户问的就是某一条，命中完整条款不截半句                |
| Markdown/技术文档 | 按标题层级切  | 500~800    | 80~120  | 标题是作者亲手画的语义边界，比算法猜得准 → 故 MD 不走 Tika |
| 长文/报告(无结构)    | 递归字符切   | 1000~1500  | 150~200 | 拿不到结构时兜底，重叠防关键句被切断                  |

- **overlap 理由**：关键句恰好落在两块交界时，重叠保证它至少完整出现在一块里（10~15%）。
- **块多大才对**：唯一准绳 = **一块内容能独立回答一个问题**。太小=断章取义，太大=多主题稀释。故禁写死 500/50。
- **新库默认**：按"规章制度"档（800/120），管理员可在设置 Tab 调。
- **面试追问"为什么不固定大小更简单？"**：固定大小对混合文档必然有一类答不准；按类型切改动成本极低、回报极高，是务实取舍。

### 5.2 检索（R2）：两条腿走路（关键词 + 语义）+ RRF 融合

**为什么必须双路**（缺一路就有一类问题必挂）：

- **只用语义向量会翻车**：问"KPI-2026 考核表"，向量觉得所有绩效内容都"语义相似"，精确编号/数字反而找不准（语义对字面精确迟钝）。
- **只用关键词会翻车**：问"离职要提前几天说"，文档写"辞职须提前30日"，字面对不上就查不到（关键词对换说法迟钝）。
- **所以双路互补**：这不是"更高级"，是"缺一路必挂一类问题"。

**hify 落地方式（重要，非照抄 Python）**：

- 语义路 = **pgvector 向量余弦**；关键词路 = **PostgreSQL 全文检索（FTS，中文用 `zhparser` 扩展分词）**。
- 一个诚实取舍：bge-m3 理论上"稠密+稀疏"一把梭能自带混合，但在 Java + pgvector 栈里，关键词那路用 PG 全文更省事 → 实际用 **bge-m3 稠密向量 + PG 全文**。
- **研究项**：确认 LangChain4j / pgvector 混合方式，或在 `PgVectorRetrievalPort` 手写 SQL 融合；若 `zhparser` 部署成本高，首版可先"向量 + 关键词高权重"近似，二期上 `zhparser`。

**为什么用 RRF 融合**：向量给余弦相似度（0~1），FTS 给 BM25 得分（无上界），**单位不同没法直接比大小** → 只比"各自榜单名次"投票，两边都靠前的块胜出。`rrf_k` 默认 60（学界默认，无需调）。

**小库降级**：<100 篇可退化为纯向量（小数据集混合检索收益近零）。

### 5.3 拒答（R3）：阈值不达标就老实说"查不到"

- 现有 `KnowledgeBase.similarityThreshold`（默认 0.6）**必须在检索时逐条真正生效**（错误模式 E4：TOP_K 与阈值是独立两步，先取 TOP_K 再阈值过滤）。
- 融合后最高分仍不及格 → 返回拒答，**绝不让 LLM 无上下文硬编**。
- **为什么**：硬塞三分像的内容给 LLM，它会编出十分像的答案——这是最危险的。准确度一半靠这条兜底。阈值须用评测集实测校准，非拍脑袋。

### 5.4 溯源（R6）：答了必附来源

- 回答标注 `[来源i]`（文档名 + 章节 + 可点击定位 chunk）。
- 这是 RAG 检索库区别于纯文档库的**核心体验**，也是"可审查/可信"的前提。

### 5.5 Query Rewriting（R4）+ Small-to-Big（R5）

- **R4 口语改写 + 多轮指代消解**：把"那婚假呢？"改写成完整书面查询；prompt 强调"保留原问题所有约束，只改表达"（错误模式 E8）；存最近 6~10 轮历史。首版可做轻量版。
- **R5 上下文补全**：命中 chunk 自动拼前 1 + 后 1 块再喂 LLM，避免断章取义；靠 `DocumentChunk.seq` 按序补全。

### 5.6 检索日志 + 拒答分析（R6 延伸，可追踪优化）

**（1）每次问答都记账（检索日志）**
- 记：问了什么 / 改写成什么 / 命中哪几块 + 分数 / 是否拒答 / 最终答案 / 耗时。
- 答偏时顺日志定位是"切坏了"还是"没找到"，这就是用户要的"方便后续追踪优化"。

**（2）拒答必须单独可分析——拒答不是失败，是"知识缺口"信号**

拒答只记一个 `rejected=true` 布尔值 = 等于没记。要能回答"**为什么拒**、**差多少**、**该补文档还是该调参**"，必须多记三样：

| 记录项 | 字段 | 作用 |
|---|---|---|
| 拒答根因 | `refusal_reason` | `NO_KB`(该 Agent 没挂任何库) / `NO_HIT`(检索完全没结果) / `BELOW_THRESHOLD`(有候选但最高分不及格) |
| 差多少 | `top_score` + `threshold` | 最高分 0.53、阈值 0.55 = **擦肩而过**（多半是阈值太严或切块问题，库里其实有）；最高分 0.1 = **库里真没有**（知识缺口，该补文档） |
| 候选留痕 | `top_candidates(json)` | 即使没过阈值，也把 TOP-3 候选块记下来，人工一看就知道"对的块到底在不在候选里" |

**（3）拒答 → 优化的闭环（这才是"方便后续优化"的落点）**
```
高频被拒的 query 聚类  →  分两类处理：
  ① 擦肩而过型(top_score 接近阈值)  →  调阈值 / 改切块策略 / 加同义词  →  再验
  ② 真空型(top_score 很低)         →  这就是"待补文档清单"，去补对应知识
```
- 体检页可加一个**「最近被拒问题 TOP-N」**列表（默认可折叠），管理员一眼看到"用户在问但答不了什么"。
- 这是把"拒答率"这个冷冰冰的指标，变成**可执行的优化动作**的唯一抓手。

---

## 6. 模型选型（已定稿）

| 用途            | 选型                           | 理由                                                  |
| ------------- | ---------------------------- | --------------------------------------------------- |
| **Embedding** | **BAAI/bge-m3（本地已装，1024 维）** | 中文第一梯队；M3=稠密+稀疏+多语言，天然契合混合检索；开源本地不出网；1024 维是效果/成本甜点 |
| **AI 作答 LLM** | **首版直接调线上 API**              | 用户本地已搭 Ollama 但暂不用，线上省算力、效果稳；后期切 Ollama 只改配置        |
| **Reranker**  | **暂不上**                      | 文档量 < 1 万，加了只增延迟+成本，不值                              |

**两条工程铁律**：

1. **LLM 必须抽象成可替换接口**（LangChain4j 天然支持）：今天填线上 API，将来换 Ollama 只改配置不动业务代码。
2. **Embedding 不可随便换**：换 embedding = 换语义空间 = 全库向量重建，是架构里**唯一不可逆**的决定 → 1024 维第一天定死，pgvector 列维度严格 = 1024。

---

## 7. 参数设计（后台可配，按"改动代价"分三层）

**核心原则**：改一个参数代价多大，决定它存哪、能不能库级改。**全项目禁止裸写 500/60/0.6 等魔法数字**（R7）。

### 🔴 第一层 建库参数（改了 = 全库重建，建库时定死）

| 参数                | 默认       | 存哪      | 说明                |
| ----------------- | -------- | ------- | ----------------- |
| `embedding_model` | `bge-m3` | 全局，禁库级改 | 换模型=全库向量作废        |
| `vector_dim`      | `1024`   | 全局，跟随模型 | pgvector 列维度，建表固定 |
| `chunk_strategy`  | `auto`   | 库级      | 按文档类型自动选切法        |
| `chunk_size`      | `800`    | 库级      | 范围 300~1500       |
| `chunk_overlap`   | `120`    | 库级      | 约 12%，范围 10~15%   |

> 改动后该库须重切+重向量化，UI 提示"修改将触发重建索引"。

### 🟢 第二层 检索参数（改了即时生效，调优主战场）

| 参数                | 默认             | 存哪           | 说明                      |
| ----------------- | -------------- | ------------ | ----------------------- |
| `retrieval_top_k` | `10`           | 全局默认+库级覆盖    | 每路各召回多少条进 RRF           |
| `final_top_n`     | `4`            | 同上           | 融合后最终喂 LLM 几块           |
| `rrf_k`           | `60`           | 全局写死         | RRF 常数，无需调              |
| `score_threshold` | `0.55`（需评测集校准） | **库级，最重要旋钮** | 拒答线                     |
| `context_expand`  | `1`            | 全局           | Small-to-Big 前后各扩几块，0=关 |

### 🔵 第三层 生成参数（LLM 侧）

| 参数                           | 默认     | 说明                 |
| ---------------------------- | ------ | ------------------ |
| `llm_provider` / `llm_model` | 线上 API | 抽象成接口，后期一键切 Ollama |
| `temperature`                | `0.1`  | RAG 要"照材料答"，压低防发挥  |
| `max_context_chars`          | `6000` | 防塞太多块爆上下文          |

**落地方式**：

```
全局默认：application.yml 的 rag: 配置段
库级覆盖：knowledge_base 表加 rag_config JSONB 字段（参数会增减，JSONB 免天天改表）
生效规则：库级有值用库级，否则回退全局默认
代码统一从一个 RagConfig 对象取值，杜绝魔法数字
```

> **日常真正会调的只有 2 个**：`score_threshold`（答不准→调）和 `final_top_n`（上下文不够/太杂→调）。首版 UI 只暴露这俩，其余藏"高级设置"，符合"别太复杂"。

---

## 8. 数据模型（对齐现有 Entity，增量落地）

> 现状（真实代码）：
>
> - `KnowledgeBase`：`name / description / embeddingModel / embeddingDim(1024) / similarityThreshold(0.6) / creatorId` + `BaseEntity`（含 `deleted` 软删）。
> - `Document`：`documentId(UUID) / kbId / title / sourceType(FILE/URL/TEXT) / sourceRef / status(UPLOADED/INDEXING/INDEXED/FAILED) / chunkCount / errorMessage` + 软删。
> - 二者已有 `@SQLDelete + @SQLRestriction(deleted=0)` 软删 → **回收站直接复用**，无需新表。

### 8.1 KnowledgeBase 增加

| 字段               | 类型         | 说明                                                                         |
| ---------------- | ---------- | -------------------------------------------------------------------------- |
| `rag_config`     | JSONB/JSON | §7 库级参数（chunk_size/overlap/strategy/top_k/final_top_n/threshold...），一个字段兜住 |
| `chunk_strategy` | enum       | AUTO / RECURSIVE / MARKDOWN_HEADER（若不放进 rag_config 则独立列）                   |
| `language`       | varchar    | zh-CN，影响分词                                                                 |
| `token_count`    | bigint     | 成本/配额可见                                                                    |
| `status`         | enum       | ACTIVE / ARCHIVED（归档；删除已由 deleted 软删覆盖）                                    |
| `is_public`      | boolean    | 可否被挂载到 Agent（§3.5 预留），默认 true；未来敏感库置 false 须 owner 授权                       |

### 8.2 Document 增加

| 字段            | 类型      | 说明                    |
| ------------- | ------- | --------------------- |
| `checksum`    | varchar | R8 去重，防重复向量           |
| `size_bytes`  | bigint  | 文件大小（列表页展示 + 20MB 校验） |
| `mime_type`   | varchar | 白名单校验记录               |
| `token_count` | int     | 单文档 token 成本          |

### 8.3 DocumentChunk（现有：docId / content / embedding）增加

| 字段      | 类型  | 说明                |
| ------- | --- | ----------------- |
| `seq`   | int | 同文档内顺序，支撑 R5 前后拼接 |
| `score` | 运行时 | 检索返回，不入库          |

### 8.4 新增表

| 表               | 用途                                                                                                                    |
| --------------- | --------------------------------------------------------------------------------------------------------------------- |
| `agent_kb_link` | **Agent ↔ KB 多对多挂载**（§3.5 核心）：`agent_id / kb_id / created_by`，唯一索引(agent_id,kb_id)                                    |
| `indexing_job`  | 异步索引任务（§3.9）：`doc_id / kb_id / batch_id(同批上传分组) / stage(UPLOAD/PARSE/CHUNK/EMBED/STORE) / progress(n/m) / status(QUEUED/RUNNING/SUCCESS/FAILED) / fail_stage / error_code / error_message / retry_count / created_at / updated_at`，支撑逐节点进度展示 + 断点重试 + 批量重试 |
| `retrieval_log` | 检索日志 + 拒答分析（§5.6）：`kb_id / agent_id / query / rewritten / hit_chunks(json) / scores / answer / cost / created_at` + **拒答专属**：`rejected(bool) / refusal_reason(NO_KB/NO_HIT/BELOW_THRESHOLD) / top_score / threshold / top_candidates(json，未过阈值的 TOP-3 也留痕)`。用于"擦肩而过 vs 知识真空"分型，驱动补文档/调参 |
| `eval_dataset`  | 题集打分（§3.8、§9）：`kb_id / question / type / keywords / expected / should_reject`                                         |

> `Document.kbId` 外键语义不动——它是"点进哪个库只看哪个库"的硬保证。
>
> **权限相关（§3.5 定稿）**：现有库级 RBAC 表 `KbAccess` **退休/大幅简化**——不再需要 viewer/editor/库成员角色；管理权只看 `KnowledgeBase.creatorId` + 全局 admin；查询权只看 `agent_kb_link`。

---

## 9. 验收标准（开发前锁定，作为门禁）

### 9.1 等级

内部 20~50 人、Agent 基于文档回答、错答有风险 → 取 **「业务系统」等级**为基线（若某库用于强合规回答，Faithfulness 上调至 90%）。

### 9.2 指标（题集打分对齐这些）

| 指标                | 目标        | 对应需求  |
| ----------------- | --------- | ----- |
| 召回率@5             | **≥ 90%** | R1/R2 |
| MRR               | ≥ 0.85    | R2    |
| NDCG@5            | ≥ 0.80    | R2    |
| Faithfulness 忠实度  | **≥ 85%** | R3/R6 |
| Answer Relevancy  | ≥ 80%     | R4    |
| Context Precision | ≥ 85%     | R2/R3 |
| Context Recall    | ≥ 85%     | R1/R2 |
| 拒答率               | ≤ 5%      | R3    |
| 幻觉率               | ≤ 10%     | R3/R6 |
| P95 延迟            | ≤ 3s      | R2/R7 |

### 9.3 前置条件

1. **建评测集**：每篇文档段落 ≥ 1 条用例；覆盖精确/语义/综合 3 类；含"文档外内容应拒答"边界用例；关键词对齐原文格式（"5 天"非"5天"，E3）。
2. 冷启动可用 LLM 自动生成 QA 对补集。
3. 阈值经实测校准，非默认盲用。

### 9.4 方式

- 检索质量：跑评测集算 Recall@5 / MRR / NDCG。
- 生成质量：RAGAS 四指标。
- 安全：注入"文档外问题"验拒答率/幻觉率。
- 性能：并发压测取 P95。
- **门禁**：未达基线不上线。

---

## 10. 错误模式规避清单（rag-knowledge → hify Java 化）

| #   | 坑                          | hify 规避                                |
| --- | -------------------------- | -------------------------------------- |
| E2  | BM25 中文无分词=废               | 关键词路用 `zhparser`（PG FTS），不裸用默认分词       |
| E1  | splitter 包路径变更             | 用 LangChain4j `DocumentSplitter`，确认包路径 |
| E3  | 评测词与文档格式不一致                | 写用例前读原文对齐格式                            |
| E4  | TOP_K 与阈值混淆                | 先取 TOP_K(10) → 再逐条阈值过滤 → 全不达标拒答        |
| E5  | pgvector metadata=None 炸   | metadata 兜底 `meta or {}`               |
| E9  | Rerank 与 Embedding 语义空间不一致 | 二期 Rerank 必同厂商                         |
| E10 | 重复导入重复向量                   | 导入前按 `checksum` 去重/upsert（R8）          |
| H5  | Embedding 模型重复加载           | Spring `@Bean` 单例，不每次 new              |
| H10 | LLM 调用无兜底                  | embed/检索/生成全 try/catch，失败友好提示          |
| H11 | 无拒答机制                      | 阈值全不达标→明确"无法回答"，不硬编                    |

> 建议把 H 类高频规则晋升写入 `AGENTS.md`，使后续 AI 编码自动遵守。

### 10.2 本项目架构特定坑（通用清单外，按选型逐条排雷）

以上 E/H 是通用坑。下面是**基于本项目实际选型**（bge-m3 稠密向量 + PG 全文 zhparser + pgvector + 线上 LLM API + Agent 挂载 + 批量异步 + Spring Boot）才会遇到的坑，按"**现阶段必做**（不做会出事故/返工）"和"**后续优化**（规模/痛点出现再做）"分层。

**🔴 现阶段必做（不做会数据泄露 / 检索全废 / 连接池打爆）**

| # | 坑 | 后果 | 现阶段解决方案 |
|---|---|---|---|
| P1 | **软删过滤没下推到向量检索 SQL** | 删了的库/文档，向量还留在 pgvector 里被检索到 → **删了还能被问出来（数据泄露级）** | 向量检索 SQL 的 `WHERE` 必须带 `deleted=0` + `kb_id in (挂载库)`，不能只在应用层过滤；`@SQLRestriction` 覆盖不到原生向量查询，要手写进 SQL |
| P2 | **敏感文档内容出网（线上 LLM API）** | 内部薪酬/制度片段被拼进 prompt 发到公网 API → **合规事故** | 上线前做数据出网评估；给库加 `is_public`/敏感标记，敏感库强制走本地 Ollama（接口已抽象，切配置即可）；日志脱敏 |
| P3 | **向量距离度量 / 归一化不一致** | 建表用 cosine 查询用 L2，或 bge-m3 向量没归一化 → **检索结果整体错乱但不报错** | 统一 `vector_cosine_ops`；bge-m3 输出统一 L2 归一化；建表、写入、查询三处度量必须一致，写进 `RagConfig` 固定 |
| P4 | **embedding/LLM 外部调用被包进数据库事务** | 向量化慢（几百 ms~秒级），事务不释放 → **DB 连接池耗尽、批量上传拖垮全站** | 外部调用严格在事务外；先写业务数据提交，再异步向量化；embedding 走独立线程池（§3.9 已定，此处强调事务边界） |
| P5 | **文档"改=重传"后旧 chunk/旧向量残留** | 新旧向量并存 → 同一问题检索到过时内容 | 重传流程固定为**先按 doc_id 删旧 chunk + 旧向量 → 再写新的**，同一事务语义保证不出现"删了没写/写了没删" |
| P6 | **zhparser 扩展不可用 / 专有名词切错** | 关键词那一路直接塌陷，混合检索退化成纯向量，精确编号(KPI-2026)查不准 | M0 阶段先验证 PG 能装 `zhparser`（部分托管 PG 装不了，装不了要及早换方案）；专有名词词典优化可后置 |
| P7 | **线上 LLM API 无超时/重试/降级** | API 抖动、限流(429)、超时 → 用户端整个问答挂死 | 设超时(如 30s)+有限重试+指数退避；限流排队；失败返回友好提示而非 500（呼应 H10） |

**🟢 后续优化（规模上来或指标不达标再做，现在做是过度设计）**

| # | 优化项 | 触发条件 | 说明 |
|---|---|---|---|
| O1 | 拒答阈值动态校准 / 分库调优 | 拒答日志积累后 | 先固定 0.55，靠 §5.6 拒答分析数据回调；别一开始就搞自适应 |
| O2 | pgvector 索引 IVFFlat→HNSW | 单库 chunk 数万级、检索变慢 | 量小时全表精确扫更准更快，**过早建近似索引反而降召回**；到量再建 |
| O3 | zhparser 自定义词典 / 同义词库 | 精确检索出现"换说法查不到"高发 | 离职=辞职这类先靠向量兜底；高发再加同义词典提关键词路 |
| O4 | retrieval_log 表分区/归档 | 日志量百万级 | 按月分区或定期归档冷数据 |
| O5 | embedding 批处理 / 结果缓存 | 上传吞吐成瓶颈 | 批量 encode + 相同文本缓存，提吞吐降成本 |
| O6 | LLM 费用监控 / 配额告警 | 线上 API 用量上来 | 记 token 消耗（retrieval_log.cost 已留字段），设月度告警 |
| O7 | Reranker 精排 | Context Precision < 85% | 与 embedding 同厂商（E9），文档 < 1 万暂不需 |
| O8 | 评测集自动回归 | 有 CI 后 | 每次改切块/检索参数自动跑 §9 门禁，防调优顾此失彼 |
| O9 | 基础 prompt 注入防护 | 文档来源不完全可信时 | 文档内容放固定引用区、system 指令置顶不可覆盖；内部可信文档优先级低 |

> **一句话分层逻辑**：现阶段只做"不做会出安全/正确性事故"的（P1~P7 全是"错了检索就废或数据泄露"）；一切"为了更快/更省/更智能"的优化（O1~O9）都等**真实规模或指标不达标**这个明确信号出现再做——这正是全文"可运维、不过度设计"的一贯取舍。

---

## 11. 开发优先级（M0 → M3）

- **M0 数据模型 + 框架**：§8 全部表/字段（KB 增量、Document 增量、agent_kb_link、indexing_job、retrieval_log、eval_dataset）+ 向量化任务框架 + `RagConfig` 参数对象。
- **M1 列表页 + 详情页(文档 Tab) + 创建(先建库→流程内可选上传)**：含**批量上传(≤10)、逐文件异步 indexing_job、逐节点进度展示、失败节点可见 + 单文件/批量重试（断点续跑）**、回收站软删。
- **M2 RAG 引擎(问答 Tab)**：切片自适应(R1) + 混合检索+RRF(R2) + 阈值拒答(R3) + 溯源(R6) + 检索日志；LLM 走线上 API 接口抽象。
- **M3 挂载 + 体检**：Agent↔KB 挂载 + 挂载权限闸门(§3.5) + 体检页 3 指标 + 试问台 + 题集打分 + 设置 Tab + `KbAccess` 简化退休改造。

**P0 地基（贯穿 M2，不做会返工）**：R1 切片自适应、R2 混合检索、R3 阈值拒答、R6 溯源、§9 评测集+门禁。

**P0 排雷（§10.2 现阶段必做，贯穿 M0~M2）**：P1 软删下推向量 SQL、P3 距离度量/归一化一致、P4 embedding 调用出事务、P5 重传删旧向量、P6 zhparser 可用性验证（M0 就要验）、P7 LLM 超时重试降级；P2 敏感数据出网评估上线前完成。

---

## 12. 首版明确不做（对齐场景约束）

| 不做                | 原因               | 何时再考虑                 |
| ----------------- | ---------------- | --------------------- |
| R9 Rerank         | 文档 < 1 万，增延迟+成本  | Context Precision 不达标 |
| 多路召回(Multi-Query) | R4+R2 已覆盖，复杂度高   | 召回率持续不达标              |
| 热点缓存              | 并发低收益小           | 上线后真实并发验证             |
| 流式响应              | 属 M6 聊天体验，非引擎阻塞项 | M6 对话阶段               |
| 多模态/扫描件 OCR       | 成本高，属"进阶"        | 出现扫描件/图表问答需求          |
| 库级 RBAC / 文档级权限   | 权限已委托给 Agent 挂载（§3.5），库只留 owner+admin 管理权 | 同库敏感文档隔离需求（先用 is_public） |
| 上传即建库 / 拖拽快捷建库    | 统一"先建库再上传"，无孤儿文件、实现更简       | 用户明确反馈建库太繁琐时          |
| 跨库全局搜索            | 库少价值有限           | 库数量增多                 |
| 多租户/计费/插件市场/评测平台  | AGENTS.md 已砍     | 永不（当前场景）              |

---

## 13. 面试/答辩速记（30 秒电梯陈述）

> "我做的是内部 RAG 知识库：文档进来先**按类型顺着天然语义边界剪成卡片**（FAQ 一问一答、规章按条、MD 按标题、长文递归+重叠），每块用 **bge-m3 转 1024 维向量**存进 pgvector。提问时**两条腿走路**——pgvector 语义向量补'换说法'、PG 全文补'精确编号'，因两路分数单位不可比，用 **RRF 按名次投票**融合。融合后**最高分不及格就拒答**，绝不让 LLM 瞎编；答了必**附来源**可溯源，每次问答**记检索日志**便于追踪优化。参数按**改动代价分三层**（建库层不可逆、检索层调优、生成层），日常只调阈值和 final_top_n。**Embedding 第一天定死不可换**（换=全库重建），**LLM 抽象成接口**（线上 API ↔ Ollama 一键切）。验收用**试问台+题集打分**，卡召回@5≥90%、忠实度≥85%、拒答率≤5%、P95≤3s。规模上来再加 Rerank/缓存/跨库——每个'暂不做'都绑了触发条件，不过度设计。"

---

*（本文为完整定稿版，整合截至 2026-07-27 的全部决策；后续如调整，直接改本文并同步 `.workbuddy/memory/`。）*
