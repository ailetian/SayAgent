# 代码审核避坑沉淀（自进化单一事实源）

本文件是「任务代码审核」skill 的坑位数据库，沉淀**代码已合入仓库后**，按 AGENTS.md 审核真实代码时反复踩到的坑。**每次审核新模块代码前通读一遍，按图索骥提前规避；发现清单外的新坑，按 SKILL.md 自进化协议追加坑位 N+1。**

> **边界**：本库只收"代码层"坑（合入后的真实代码违规）。计划文档阶段的坑（章节号错位、Flyway 版本、包路径等）归 `../子模块任务拆分验收/references/验收踩坑沉淀.md`。

---

## 坑位索引

| 编号 | 标题 | 严重级 |
|------|------|--------|
| 1 | 验收只验文档不验代码（test green ≠ 合规） | P1 |
| 2 | 测试连真实库违反 §7.10 规则35 | P1 |
| 3 | 软删除 `deleted=0` 过滤未真生效（§6.1） | P1 |
| 4 | 代码注释错引 AGENTS.md 章节 | P3 |
| 5 | 软删注解放 @MappedSuperclass 不传播（须放 @Entity） | P1 |
| 6 | "改完代码/lint 绿"就报"审核通过"，没实跑测试 | P0 |
| 7 | Pg/第二数据源表用 `@PostConstruct` 运行时建表，绕开 Flyway（§9） | P0 |
| 8 | pgvector 向量列缺 HNSW 索引，检索全表扫描（§6.6/§6.7） | P0 |
| 9 | 设计偏离需对照 `06_Mn` 全局验收，而非仅 T1–Tn（tenant_id→kb_id） | P1 |
| 10 | 第二数据源 `@ConfigurationProperties` 前缀写错 + 用 @PostConstruct 触发第二 Flyway 致上下文启动失败 | P0 |
| 11 | 验收点要求的阈值/过滤未接入业务链路，配置成死代码（静默失效） | P1 |
| 12 | 改接口/方法签名或新增方法后 maven 增量编译不重编依赖类，旧 class 抛 NoSuchMethodError（P1） | P1 |
| 11 | 用 `@SpringBootTest` 测 controller 鉴权 401，把整库上下文拉起来（headless 必败） | P0 |
| 13 | 编排类服务用 @Mock 全 mock 外部依赖，跨模块 import 内部类被"假合规"掩盖 | P1 |
| 14 | 控制器 currentUser() 把 principal 强转成 User，但 AuthFilter 只塞 username 被 @WebMvcTest 吞异常掩盖 | P0 |
| 15 | Git Bash/MinGW 下 mvn 报 ClassNotFoundException 构建跑不起来 | P2 |
| 16 | 前端 vite build 被 WorkBuddy safe-delete 拦截 trash dist 而误报"Build failed"（非代码错误） | P2 |
| 17 | controller HTTP 冒烟：@WithMockUser 需 spring-security-test（本项目 starter-test 未传递引入），@WebMvcTest 编译不过 | P2 |
| 18 | 远程调用边界类 `catch (Exception e)` 一把抓，违反 §7.3 规则11 且与 modelprovider 惯例不一致；内部已统一转 BizException 时外层该分支多为死代码 | P2 |
| 19 | 改编排类服务构造器签名 → 同包既有测试 `new Xxx(...)` 编译失败（漏改参） | P1 |
| 20 | 无 K8s 集群时 `kubectl --dry-run=client` 仍强制 discovery 连 server 报 failed to download openapi；纯本地校验需 kubeconform/或离线佐证 | P2 |
| 21 | docker-compose 相对路径以 compose 文件所在目录为基准，`config` 不校验宿主路径存在性 → 挂载/build context 错位静默漏过 | P0 |
| 22 | 死 `catch (Exception e)` 包裹根本不抛受检异常的代码（如 `new String(charset)`），不可达且违反 §7.3 规则11 | P2 |
| 23 | 注册型常量（ErrorCode/枚举）定义但零引用 = 死代码，"新增 N 项"验收点假达标 | P2 |
| 24 | 自定义构建/验收脚本的 `rm -rf` 被 Bash 沙箱 safe-delete 拦截，阻断「实跑」结论产出 | P2 |
| 25 | 跨库两段式 IN 下推：id 类型与 PG chunk 表口径不一致，混合检索零命中 | P0 |
| 26 | 异步编排器顶层 catch(Exception) 是必需兜底，勿误判规则11违规；JSON 序列化 catch 仍须收窄 IOException（同#18/#22）；复制粘贴致悬空/重复 Javadoc | P3 |
| 27 | 单测 classpath 回补部署产物缺失的传递依赖，测试绿掩盖线上故障（无 Maven 环境 fat jar 缺 poi-scratchpad 等） | P1 |
| 28 | 无 Maven 环境手工注入部分 K0808 类到运行实例，混合新旧类状态掩盖其他未注入类的回归（e2e 假绿） | P2 |
| 29 | 手工注入含嵌套内部类（枚举/record）的源文件漏拷独立 .class → 启动 ClassNotFoundException | P0 |
| 30 | native query 传枚举参数默认按 ordinal 绑定，违反 §7.2（须传 enum.name() 字符串） | P1 |
| 31 | controller standaloneSetup 测 HTTP 不加载安全链，@PreAuthorize 角色闸门不生效，RBAC 须下沉 service 抛 FORBIDDEN | P1 |

---

## 坑位 1：验收只验文档不验代码（test green ≠ 合规）（P1）

**现象**：M3/T1 验收报告判定"整改后通过"，但真实代码存在 D1（测试 `@DataJpaTest` 连真实 MySQL，违反 §7.10 规则35）、D2（`BaseEntity` 无 `@Where`/`@SQLRestriction`，`deleted=0` 过滤未生效，违反 §6.1）、D3（仓库 Javadoc 错引 `§7.8` 描述派生查询）。

**真相（以仓库/AGENTS.md 为准）**：验收只核对 `plans/M3/*.md`（计划文档）与 AGENTS.md，从未 `read_file` 真实仓库文件做"代码 ↔ 规范"核对；把"代码已 merge"等同于"代码合规"，三方一致性里的"代码"方被跳过。这是缺陷漏过验收的**元级根因**。

**排查命令**：
- 对每个 T*，审核前 `list_dir`/`read_file` 真实文件，逐条对照 AGENTS.md 相关规则（不只看计划文档）。
- 审核点改写为"规范符合性检查"：如"测试是否连真库""软删是否真生效"，而非"用例绿即可"。

**教训**：审核必须验实际代码，不得假设已合入即合规；**test green ≠ 合规**。

**来源**：M3/T1 代码审核 2026-07-22

---

## 坑位 2：测试连真实库违反 §7.10 规则35（P1）

**现象**：仓储测试 `@DataJpaTest` + `@AutoConfigureTestDatabase(replace = NONE)` 连真实 MySQL，与 §7.10 规则35"测试不依赖真实外部资源（DB/LLM/网络）"冲突。

**真相/处理**：规则35原意针对纯单元测试；`@DataJpaTest` 切片测试需真实 DB 才能验 JPA 派生查询。已在 AGENTS.md §7.10 规则35 显式补充"切片/集成测试例外"（事务内执行、结束自动回滚、不污染业务数据）。直连真库开发时，按此例外登记即可合规。纯单元测试（Service/Util 逻辑）仍须 Mockito Mock，禁止连真库。

**排查命令**：`search_content` 在测试类搜 `@AutoConfigureTestDatabase|@DataJpaTest|replace = NONE`，确认是否连真库，且**已在规范登记例外**——二者必居其一。

**教训**：审核仓储测试类时，查"是否连真库 / 是否已按规则35例外登记"二者必居其一，否则违规。

**来源**：M3/T1 代码审核 2026-07-22

---

## 坑位 3：软删除 `deleted=0` 过滤未真生效（§6.1）（P1）

**现象**：`BaseEntity` 仅有 `deleted` 字段，查询返回已软删行，违反 §6.1"查询加 `WHERE deleted=0`，不真删数据"；`V4` 为"含 `deleted` 联合索引"设计的 `idx_type(type, deleted)` 第二列永远用不上。

**真相（以仓库为准）**：全库 `search_content` `@Where|@SQLRestriction|deleted = 0` **0 命中**——软删机制从未真正实现。这是 common 底座存量缺口，被验收漏掉（只因文档说"§6.1"就默认生效）。

**修复（重要纠偏）**：**不能**在 `BaseEntity`（`@MappedSuperclass`）上加 `@SQLRestriction`/`@Where`/`@SoftDelete`——本项目实测这类注解放 MappedSuperclass **不会传播到子类查询**（详见坑位5）。可靠做法：把 `@SQLRestriction("deleted = 0")` + `@SQLDelete(sql = "UPDATE <表> SET deleted = 1 WHERE id = ?")` **直接加在具体 `@Entity` 类上**（详见坑位5），并写一个"save 一条 deleted=true 再查询断言被过滤"的测试 + 看 Hibernate SQL 确认 `deleted = 0` 真的出现。仅看"测试绿"会漏（测试数据 deleted 默认 false，过滤不生效也绿）。

**排查命令**：`search_content` 在 `backend/src/main/java` 搜 `SQLRestriction|@Where|deleted = 0`，确认软删过滤已落地；对每张含 `deleted` 的表核对。

**教训**：审核 common 底座时，核对软删机制"是否真生效"，不仅是"字段存在"。

**来源**：M3/T1 代码审核 2026-07-22

---

## 坑位 4：代码注释错引 AGENTS.md 章节（P3）

**现象**：仓库 Javadoc 写"§7.8 规则30/31"描述派生查询，但该章实为控制语句（必须用 `{}`、禁止 if 内赋值），与派生查询无关。

**真相（以 AGENTS.md 为准）**：§7.8=控制语句；派生查询/仓库命名应引 §7.1（命名）/§3.2（仓库）。代码注释错引章节同样误导后续维护者。

**排查命令**：`search_content` 在代码注释搜 `§7\.8|§6\.2|规则30|规则31`，核对引用章节主题是否匹配文档/代码实际内容。

**教训**：代码注释里的 AGENTS.md 章节引用也要核对，错引与文档错引同害。**高频混淆点（重点）**：`§7.3 = 异常处理（规则10–14）`、`§7.4 = 日志（规则15–19）`——凡是讲"异常 / 抛 `BizException` / 规则10-14 / FORBIDDEN"的注释都必须引 **§7.3**，绝不引 §7.4；凡讲"WARN/INFO 落日志 / 规则15-19"才引 §7.4。M3/T5 的 `ModelService`、路由层 `ResilienceDecorator`/`ProviderRouter` 都踩过把"异常"误写成 §7.4 的坑。同理 `§3.1 = 仓库顶层`、`§3.2 = 后端包结构`、`§3.4 = 分层纪律`、`§3.5 = 响应契约`，`client/` 与 `dto/` 的归属应引 §3.2/§3.4 而非 §3.1。

**来源**：M3/T1 代码审核 2026-07-22；M3/T5 复验 2026-07-23（同模式再次命中）

---

## 坑位 5：软删注解放 @MappedSuperclass 不传播（必须放 @Entity）（P1）

**现象**：在 `BaseEntity`（`@MappedSuperclass`）上加 `@SQLRestriction("deleted = 0")` / `@Where(clause="deleted = 0")` / `@SoftDelete`，期望"全表自动软删"，但子类（如 `ModelProvider`）的派生查询 SQL 里**完全没有 `deleted = 0` 过滤**；其中 `@SoftDelete` 类级还会与子类 `deleted` 属性冲突报 `MappingException: Column 'deleted' is duplicated`。

**真相（以实跑验证为准，Hibernate 6.5.3 + Spring Boot 3.3）**：软删注解（@Where / @SQLRestriction / @SoftDelete）放在 `@MappedSuperclass` 上，**不会传播到子类查询**——类级、字段级都不行。可靠方案：把过滤与删除转更新直接加在**具体 `@Entity` 类**上（已实跑验证生效，M3/T1 最终落地）：
```java
@SQLRestriction("deleted = 0")   // 用 value：@SQLRestriction("...")；勿写 @SQLRestriction(clause="...")（clause 是 @Where 的属性，写错抛 IncompleteAnnotationException）
@SQLDelete(sql = "UPDATE model_provider SET deleted = 1 WHERE id = ?")
@Entity
@Table(name = "model_provider")
public class ModelProvider extends BaseEntity { ... }
```
- `@SQLRestriction`（Hibernate 6.3+ 替代 `@Where`，非废弃）在**实体类级**生效；`@Where` 也生效但自 6.3 起废弃，故优先用 `@SQLRestriction("deleted = 0")`。
- `@SoftDelete` 是 Hibernate 6.5 软删专用注解，但本项目验证其在 MappedSuperclass 上同样不传播、且类级会与 `deleted` 属性重复映射（`Column 'deleted' is duplicated`），**暂不采用**。

**排查命令**：
- 写一个回归测试：save 一条 `setDeleted(true)` 的行，再 `findByXxx`/`findAll` 断言返回结果不含它；看 Hibernate 打印的 SQL 是否含 `where (mp1_0.deleted = 0) and ...`。**注意 L1 缓存陷阱**：同 `@DataJpaTest` 事务内用 `findById(id)` 会命中一级缓存、直接返回已管理的实例而**不**发 SQL，导致 `@SQLRestriction` 过滤被绕过、误判"未生效"；验证过滤应用 `findAll()`/派生查询（发 SQL 带 `deleted=0`）而非 `findById`（User 软删补全时踩过，已修正为 `testFindAll_excludesSoftDeletedUser`）。
- `search_content` 在 `backend/src/main/java` 搜 `@Where|@SQLRestriction|@SQLDelete|@SoftDelete`，确认这些注解在**实体类**（而非 BaseEntity）上。
- 仅看"测试绿"不够：测试数据 `deleted` 默认 false，过滤没生效也会绿。

**教训**：软删过滤必须放在 `@Entity` 类上并用"带软删数据的查询测试 + 看 SQL"验证真生效；放 `@MappedSuperclass` 上是"假生效"。审核软删时，绝不假设"BaseEntity 有 deleted 字段 = 已软删"。

**来源**：M3/T1 代码审核 2026-07-23（D2 原以为 @SQLRestriction 在 BaseEntity 修好，实跑才发现查询 SQL 无 `deleted=0`，连改三次才落地为 @Entity 级 @Where+@SQLDelete）

---

## 坑位 6：没实跑测试就报"审核通过"（P0 · 刚性红线）

**现象**：AI 改完代码、看一眼 lint/构建无错，就向用户回复"M3/T1 代码审核测试没问题了""审核通过"。实际根本没执行测试，更没看测试 SQL；后来实跑才发现软删过滤"假生效"（坑位5）——之前那句"没问题"是空的。

**真相（以实跑为准）**：审核结论**必须**来自真实跑过的测试输出（如 `mvn test` 结果、`Tests run: N, Failures: 0`、Hibernate 打印的 SQL 含/不含 `deleted = 0`），而不是"代码改完、看起来对"。"我改了 + lint 绿"只说明**可能能编译**，连"功能对"都未验证，遑论"符合规范"。本 skill 的"test green ≠ 合规"（坑位1）是下限；**更下限是：连 green 都没跑出来，就绝不允许说"通过"**。

**排查命令 / 操作纪律**：
- 任何"审核通过"类结论前，必须先实跑相关测试：如 `cd backend && mvn -q -Dtest=ModelProviderRepositoryTest test`，并把 `Tests run / Failures / Errors` 数字与关键 SQL 贴给用户。
- 软删/规则35例外等"假生效"高发点，除用例绿外，还要 `search_content` 确认注解位置正确 + 看 SQL 确认过滤真的出现（见坑位3/5）。
- 若环境暂不能跑（无 DB / 网络），**明确告知用户"未实跑、结论待验证"**，不得含糊报通过。

**教训**：没跑测试 = 没审核。结论里写"通过/没问题"前，先问自己"我刚刚实跑了吗？输出贴出来了吗？"——没跑就写，是这条 P0 红线违规。

**来源**：M3/T1 代码审核复盘 2026-07-23（用户质问"都没测试怎么能说审核测试通过？"）

---

## 坑位 11：验收点要求的阈值/过滤逻辑未接入业务链路，配置项成死代码（静默失效）（P1）

**现象**：M5 T4 验收点3 / 06_M5 全局验收要求"检索时过滤低于相似度阈值的 chunk"；`application.yml` 配了 `hify.embedding.similarity-threshold: 0.6`、`KnowledgeBase` 实体也有 `similarity_threshold` 列，但检索代码（`PgVectorRetrievalPort.retrieve` / `KnowledgeService.retrieve`）从未引用该值，只做 `ORDER BY score DESC LIMIT topK`。低于阈值的噪声 chunk 被原样返回；配置与列"看起来都在"，验收点却被静默绕过、不报任何错。

**真相（以实跑为准）**：验收点里"阈值过滤"这类**软性能力**不像 §9 硬编码建表那样会启动失败，漏接时静默失效，极易在"配置/列已定义"的假象下通过审核。必须追到"业务链路是否真的用这个值过滤"：要么检索 SQL 加 `AND 1 - (embedding <=> ?) >= ?`，要么 service 层对返回 chunk 按 `score >= threshold` 过滤，阈值来源应是 per-KB 的 `KnowledgeBase.similarity_threshold`（缺省回退 `EmbeddingConfig`）。`search_content` 搜配置项名确认"声明处"和"使用处"都要有命中——只有声明 = 死代码。

**排查命令**：`search_content` 配置项/列名（如 `similarityThreshold|similarity-threshold`），确认除声明外，检索/装配链路也有引用；无引用即死代码。对每条"阈值/过滤/上限"类验收点，反向追到真正消费它的代码行。

**教训**：验收点中"过滤/阈值/上限"类软性能力，必须验证其被业务链路真正消费，不能因"配置项/DB 列已存在"就默认生效——静默失效比启动失败更危险。

**来源**：M5 T4 代码复审核 2026-07-24

---

## 坑位 12：改接口/方法签名或新增方法后，maven 增量编译不重编依赖类，旧 class 运行时抛 NoSuchMethodError / IncompatibleClassChangeError（P1）

**现象（M5 整改复验实测）**：给 `RetrievalPort.retrieve` 加 `threshold` 参数（接口+实现类都改了），跑测试却报 `retrieve 需要 3 参数`、或 `java.lang.Error`（NoSuchMethodError：RetrievalPort.retrieve(float[],Long,int)）。`mvn ... test` 日志显示 `Nothing to compile - all classes are up to date`——增量编译没重编 `KnowledgeService` 等依赖类，target/classes 里仍是旧 .class；新接口与旧调用方不匹配，运行期才炸。改测试 import、加方法同理，会出现"代码看着对、编译报旧符号"的诡异现象。

**真相（以实跑为准）**：改**接口签名 / 新增接口方法 / 跨模块新增方法**这类影响多个编译单元的结构性改动后，maven-compiler-plugin 3.x 增量编译（useIncrementalCompilation）有时基于 staleness 误判"无需重编"，旧 class 被沿用；test-compile 又能用新 src 通过（掩盖问题），surefire 运行期才暴露 NoSuchMethodError。

**排查/修复**：结构性改动后直接 `mvn -o clean test`（或 IDE Rebuild Project）强制全量重编，不要信增量 "up to date"。CI 一般用 clean 不受影响，本地增量跑测试尤其要手动 clean。

**教训**：凡改了接口/抽象方法签名、或新增会被多处调用的方法，跑测试前先 `mvn clean`，否则增量编译的"假绿/假红"会误导判断（本次一度误以为 RetrievalPort 没改成功）。

**来源**：M5 T4 阈值接线复验 2026-07-24

---

## 坑位 17：controller HTTP 冒烟测试缺 spring-security-test（@WithMockUser 编译不过）（P2）

**现象**：M7/T1 写 `McpServerControllerTest` 用 `@WebMvcTest` + `@Import(SecurityConfig.class)` + `@WithMockUser(roles="ADMIN")` 验「已登录 ADMIN 调 POST 返回 Result」，编译报错 `cannot find symbol WithMockUser`（`org.springframework.security.test.context.support.WithMockUser` 无法解析）。

**真相（以实跑为准）**：`@WithMockUser` 来自 `spring-security-test`；本项目 `pom.xml` 只有 `spring-boot-starter-test`，**它不传递引入 `spring-security-test`**（已查 `.m2` 与 starter-test 的 pom 确认），故测试编译期该类不在 classpath。`@WebMvcTest`/`@MockBean` 来自 `spring-boot-test` 可用，但 `@WithMockUser` 单独缺失。

**排查命令**：`grep -n "security-test" pom.xml` 确认是否显式声明；`find ~/.m2/repository/org/springframework/security/spring-security-test -name "*.jar"` 确认本地是否缓存。

**正确做法（二选一）**：
1. 不引入新依赖、零外部依赖：用 `MockMvcBuilders.standaloneSetup(new XxxController(mockService)).build()` 隔离装配，手动 mock service，断言 `Result{code:0}` 与 `@Valid` 400；401/403 闸门交由 `SecurityConfig` + 服务层单测覆盖（M7/T1 即采用此法，3 测全绿）。
2. 若要真走安全链（如验 401/403 经 filter）：在 `pom.xml` 显式加 `<dependency><groupId>org.springframework.security</groupId><artifactId>spring-security-test</artifactId><scope>test</scope></dependency>`（需网络拉取），再用 `@WithMockUser`。

**教训**：本仓库 controller HTTP 冒烟优先用 `standaloneSetup`（无依赖、无 DB/全上下文），避免 `@WithMockUser` 因缺 `spring-security-test` 编译失败；确需安全链时显式加该依赖。

**来源**：M7/T1 代码审核 2026-07-25

---

## 坑位 18：远程调用边界 `catch (Exception e)` 一把抓，违反 §7.3 规则11 且与项目惯例不一致（P2）

**现象（M7/T2 实测）**：`mcp/McpClientManager.java` 的 `listTools`/`callTool`/`getTransport` 用 `catch (Exception e)` 一把抓，把异常统一包成 `BizException(MCP_CALL_FAILED)`。但同一文件里 `HttpTransport`/`StdioTransport` 早已把所有网络/超时/解析/协议错误转成 `BizException(MCP_CALL_FAILED)` 并在外层 `catch (BizException e) { throw e; }` 先接住，导致 manager 层的 `catch (Exception e)` 第二分支是**死代码**（非 BizException 永不出现）。

**真相（以实跑/通读为准）**：AGENTS.md §7.3 规则11「捕获具体异常，不写 `catch (Exception e)` 一把抓；多 catch 从具体到宽泛」。本项目远程调用客户端（`modelprovider/client/impl/OpenAiClient`、`OllamaClient`）全部只 `catch (IOException e)`（OkHttp 抛的就是 IOException 子类，含 SocketTimeoutException），**没有**一处 `catch (Exception e)`。McpClientManager 与之不一致，且违规。更隐蔽的是"死 catch(Exception) 分支"——当内层已统一转 `BizException` 后，外层 `catch (Exception e)` 既不接到新类型、又让"自身 bug（如 NPE）"被伪装成 MCP 失败，违背 §7.3 规则12「异常仅用于真正异常」。

**排查命令**：
- `search_content` 在 `src/main/java/com/hify/hify` 搜 `catch (Exception e)`，逐一审视：是否真需要宽捕；若同方法已有 `catch (BizException e)` 且被调方已将错误统一转 BizException，则该宽捕为死代码，删之。
- 对照 `modelprovider/client/impl/*Client.java` 的 `catch (IOException e)` 惯例，确认新建远程边界类的 catch 粒度。

**正确做法**：远程边界类 `catch (IOException e)`（或具体异常）后转 `BizException`；上层编排类只 `catch (BizException e) { log.error(..., e); throw e; }` 补记日志，不补 `catch (Exception e)`。自身 bug（NPE 等）应向上暴露由全局异常处理，而非伪装成 MCP/远程失败。

**教训**：远程边界 catch 粒度要对齐项目惯例（本项目：具体 IOException），且"内层已统一转 BizException"时外层别再 `catch (Exception e)`——那是死代码且会掩盖自身 bug。JSON 序列化辅助（`objectMapper.writeValueAsString` 仅抛 `JsonProcessingException`）等同此类，也须收窄到具体异常，勿 `catch (Exception e)` 一把抓——这是「报告称零违规、深度复验却漏判」的高发点（K5 复验 2026-08-05 实测）。

**来源**：M7/T2 代码审核 2026-07-26

---

## 坑位 19：改编排类服务构造器签名 → 同包既有测试 `new Xxx(...)` 编译失败（漏改参）（P1）

**现象（M7/T3 实测）**：给 `ConversationService` 构造器加第 10 个参数 `McpService mcpService`（接入 MCP 能力），`mvn test` 立即报 `ConversationServiceTest.java:100` / `ConversationServiceStreamTest.java:97`「实际参数列表和形式参数列表长度不同」——两个 M6 既有测试用旧 9 参 `new ConversationService(...)` 直接构造，未同步补参，testCompile 失败，连"测试绿"都到不了。

**真相（以实跑为准）**：给编排类 `@Service` 加构造参数（如接入新能力）会改变其构造器签名；同包/同模块的既有单测若用 `new Xxx(...)` 直接构造（非 Spring 容器注入），必须同步补参，否则 testCompile 阶段就失败。这与坑位12（增量编译 stale class → 运行期 NoSuchMethodError）同源但表现不同：此处是**编译期**测试失败，且只影响"直接 `new` 构造"的测试，不影响经 Spring 注入的生产代码。增量编译也救不了——testCompile 会因参数数不匹配直接报错。

**排查命令**：
- 改编排类构造器签名后，`search_content` 全仓搜 `new <XxxService>(` 列出所有直接构造点（含 test），逐一核对是否需补参。
- 补参后 `mvn clean test` 强制全量重编确认（避免增量掩盖，见坑位12/15）；本例补 `mcpService` 后 3 个 ConversationService* 测试 10 测全绿。

**教训**：凡给编排类服务加构造参数，先 grep 所有 `new XxxService(` 同步补参，再 clean 跑测；别等 testCompile 报「参数列表长度不同」才回头补。

**来源**：M7/T3 代码审核 2026-07-26

---

## 坑位 20：无 K8s 集群时 kubectl --dry-run=client 仍强制 discovery 连 server 报 failed to download openapi（P2）

**现象**：M7/T4 验收点3 要求 `kubectl apply -f deploy/k8s/ --dry-run=client` 通过。本机无 K8s 集群时直接报 `error validating data: failed to download openapi: Get "http://localhost:8080/openapi/v2": dial tcp ... refused`；加 `--validate=false` 仍报 `unable to recognize ... Get http://localhost:8080/api: refused`。并非清单语法错（kubectl 已成功 parse 全部 YAML，否则会先报 parse 错），而是 kubectl 默认需连 server 做 API discovery（GVK→resource 映射）。

**真相（以实跑为准）**：kubectl v1.34 的 client-side dry-run 仍会做 API discovery，无集群即失败。纯本地离线校验需改用 `kubeconform`/`kubeval`（基于 OpenAPI schema 本地校验），或接受 "kubectl parse 无错 + YAML 合法" 作为离线佐证。生产环境（有集群）`kubectl apply --dry-run=client` 应通过。

**排查命令**：
- 无集群时验证 K8s 清单：`kubectl apply -f deploy/k8s/ --dry-run=client` 报错信息若为 `failed to download openapi`/`unable to recognize`（discovery 连接错）即非语法错；可单文件 `kubectl apply -f x.yaml` 观察首个错误是否 parse 错。
- 离线 YAML 合法性：`docker compose config`（compose 直接校验）+ kubectl 单文件报错阶段判断（discovery 前无 parse 错即 YAML 合法）。
- 真离线 schema 校验装 `kubeconform`：`kubeconform -strict deploy/k8s/*.yaml`。

**教训**：无 K8s 集群环境，kubectl dry-run 无法直接连 server 校验；验收点3 的"连集群 dry-run"需在有集群环境最终确认，本环境以"kubectl parse 通过 + YAML 合法"佐证并如实标注（坑位6 透明纪律），不得谎报通过。

**来源**：M7/T4 代码审核 2026-07-26

---

## 新增坑位模板（自进化用，追加到本文件末尾）

```
## 坑位 N+1：<一句话标题>（<严重级 P0/P1/P2/P3>）

**现象**：<在代码审核中观察到的错误表现>
**真相（以仓库/AGENTS.md 为准）**：<根因与正确做法>
**排查命令**：<可复用的 search_content/list_dir/read_file 命令>
**教训**：<一句话可复用结论>
**来源**：<发现于哪次代码审核，如 Mx/Ty 2026-MM-DD>
```

> 追加后别忘了更新顶部"坑位索引"表，并向用户说明新增了哪个坑位。

---

## 坑位 7：Pg/第二数据源表用 `@PostConstruct` 运行时建表，绕开 Flyway（§9）（P0）

**现象**：M5 Knowledge 模块的 `DocumentChunkRepository` 用 `@PostConstruct createTableIfNotExists()` 执行 `CREATE EXTENSION IF NOT EXISTS vector` + `CREATE TABLE IF NOT EXISTS document_chunk` + `CREATE INDEX`，把表结构直接写死在 Java 启动逻辑里。仓库 `resources/db/` 下只有 `mysql/`，无 `pg/` 目录，`application.yml` 的 `spring.flyway.locations` 也只指向 `classpath:db/mysql/migration`。

**真相（以 AGENTS.md 为准）**：AGENTS.md §9「数据库表结构变更一律走 Flyway……AI 禁止生成 `ALTER`/`CREATE TABLE` 等直接连库执行的语句」。第二数据源（如 Pg 向量库）的表**同样**必须落在 Flyway 迁移脚本里，不能用 `@PostConstruct` + `jdbcTemplate.execute("CREATE TABLE...")` 落地。注意 Spring Boot 的 `Flyway` 自动配置默认只管一个 location/一个数据源，第二数据源需**独立 Flyway 实例**（或 `Flyway` 手动 bean 指向 `db/pg/migration`），否则迁移根本不会跑。

**排查命令**：
- `search_content` 在代码搜 `@PostConstruct` + `CREATE TABLE|CREATE INDEX|CREATE EXTENSION`，定位运行时建表。
- `list_dir` `backend/src/main/resources/db/`，确认是否同时存在 `pg/migration`（或第二数据源对应的迁移目录）。
- 核对 `application.yml` 的 `spring.flyway.locations` 是否覆盖所有数据源。

**教训**：任何"建表/加索引/CREATE EXTENSION"都应进 Flyway 迁移；代码层出现 `@PostConstruct` + `CREATE TABLE` = 直接违反 §9，是 P0 阻断项。第二数据源要专门配 Flyway，别以为"只管主库"就合规。

**来源**：M5 T1/T4 代码审核 2026-07-24

---

## 坑位 8：pgvector 向量列缺 HNSW 索引，检索全表扫描（§6.6/§6.7）（P0）

**现象**：`document_chunk.embedding vector(1024)` 列已建，但建表逻辑（无论运行时还是迁移）只建了 `kb_id`/`document_id` 的普通 B-Tree 索引，`PgVectorRetrievalPort.retrieve` 用 `ORDER BY 1 - (embedding <=> ?::vector) LIMIT ?` 做余弦检索。无 HNSW 时 pgvector 退化为**精确全表扫描**。

**真相（以 AGENTS.md 为准）**：AGENTS.md §6.6「pgvector 向量列必须显式建与检索算子匹配的 HNSW 索引，禁止裸向量列无索引」；§6.7「检索必须走向量索引，禁止全表扫描」。`<=>` 是余弦距离算子，检索用 `1 - (embedding <=> ?)` 必须配套 `USING hnsw (embedding vector_cosine_ops)` 索引，否则数据量一大性能雪崩。

**排查命令**：
- `search_content` 在 `*.sql` 与代码建表处搜 `hnsw|vector_cosine_ops`，确认向量列有 HNSW 索引。
- 实跑后 `EXPLAIN` 检索 SQL：应见 `Index Scan using idx_..._embedding`，而非 `Seq Scan on document_chunk`。

**整改样板**：
```sql
CREATE INDEX idx_document_chunk_embedding
  ON document_chunk USING hnsw (embedding vector_cosine_ops)
  WITH (m = 16, ef_construction = 64);
```

**教训**：凡出现 `vector(n)` 列，迁移里必须配套 HNSW 索引；没索引的余弦检索是 P0 性能阻断，且违反 §6.6/§6.7。

**来源**：M5 T4 代码审核 2026-07-24

---

## 坑位 9：设计偏离需对照 `06_Mn` 全局验收，而非仅 T1–Tn（tenant_id→kb_id）（P1）

**现象**：M5 计划 T1/T4 与 `06_M5` 全局验收明确要求 `tenant_id`（含"预留多租户，检索仍按 tenant_id 过滤"）；真实代码删掉了 `tenant_id`，改用 `kb_id` 做隔离维度（document.kb_id、document_chunk.kb_id、检索 `WHERE kb_id = ?`）。功能上隔离仍生效，但丢失多租户预留能力，且与模块全局验收字面不符。

**真相（以实跑为准）**：计划 T1–Tn 常只描述"本子任务"的字段，而"全局验收门槛"（如 `06_M5` 里的 tenant_id/多租户/HNSW/预留能力）写在单独文档。代码层审核若只比对 T1–T5，会漏掉这类"功能对、契约偏"的偏离。SQL 用 `?` 占位符参数化（规则7 防注入 ✅），所以不是注入问题，而是**验收契约偏离**问题。

**排查命令**：
- 审核每个模块时，`list_dir plans/Mn` 找 `06_Mn_*验收*.md` 或模块总览里的"全局验收"，逐条与真实 schema/SQL 比对。
- 重点核对：隔离维度（tenant_id/kb_id/org_id）、预留多租户、HNSW/索引、跨模块字段约定。

**教训**：代码层审核须**额外比对 `06_Mn` 全局验收门槛**，不能只看 T1–Tn；发现"kb_id 替代 tenant_id"这类设计偏离时，标记为 P1 偏离并提请产品/架构确认，而非默认通过。

**来源**：M5 T1/T3/T4/T5 代码审核 2026-07-24

---

## 坑位 10：第二数据源 `@ConfigurationProperties` 前缀写错 + 用 @PostConstruct 触发第二 Flyway 致上下文启动失败（P0）

**现象（M5 整改实测）**：把 Pg 向量库表结构改成 Flyway 后，触发迁移时遇到一连串启动失败：
1. 第二数据源 bean 用 `@ConfigurationProperties("spring.pg")` + `DataSourceBuilder.create().build()`，但 yml 实际键是 `spring.datasource.pg`（且用 `jdbc-url`），导致 `jdbcUrl` 为空，Hikari 报 `jdbcUrl is required with driverClassName`。
2. 迁移放在 `@Configuration` 类的 `@PostConstruct` 里直接 `flyway.migrate()`，而该 @Configuration 同时定义了主 `@Primary DataSource`；主 Flyway(`flywayInitializer`) 创建时要拿主 DataSource → 触发该 @Configuration 实例化 → 其 @PostConstruct 又要建第二数据源/跑迁移 → **循环依赖**（`BeanCurrentlyInCreationException: pgVectorConfig`）或级联拖垮主 Flyway。
3. `@PostConstruct` 方法带参数注入 DataSource 编译/运行报错（`Lifecycle annotation requires a no-arg method`）。

**真相（以实跑为准）**：
- 第二数据源的 `@ConfigurationProperties` 前缀**必须和 yml 完全一致**：yml 是 `spring.datasource.pg`，代码就得写 `@ConfigurationProperties("spring.datasource.pg")`，不是 `spring.pg`。且用裸 `DataSourceBuilder` 时 `jdbc-url` 绑不上 `url`（DataSourceBuilder 只有 `url` 字段），应用 `new HikariDataSource()` + `@ConfigurationProperties` 才能把 Hikari 风格的 `jdbc-url` 正确绑到 `jdbcUrl`。
- 第二数据源的 Flyway **不要放在 @PostConstruct**（会与自动 Flyway/主 DataSource 的 Bean 创建互相阻塞、还可能循环依赖）。应放在 `ApplicationRunner`（上下文刷新后再执行），且 pg 不可达时只告警不抛异常，保证无需真实 pg 也能启动（如用 @MockBean 规避真实建库的 @SpringBootTest）。

**排查命令**：
- `search_content` `CREATE TABLE|CREATE EXTENSION|@PostConstruct` 在 `**/config/*.java` 与 `**/*Repository.java`，确认是否运行时建表。
- 跑 `@SpringBootTest` 全上下文测试（如 `KnowledgeControllerAuthTest`），若出现 `jdbcUrl is required` / `BeanCurrentlyInCreationException` / `pgVectorConfig`，即命中本坑。
- 核对 yml 第二数据源前缀与代码中 `@ConfigurationProperties(...)` 是否逐字一致。

**整改样板**：
```java
// PgDataSourceConfig.java
@Bean(name = "pgDataSource")
@ConfigurationProperties("spring.datasource.pg")   // 前缀必须 = yml 的 spring.datasource.pg
public DataSource pgDataSource() {
    return new HikariDataSource();                 // Hikari 风格 jdbc-url 才能正确绑定
}

// PgSchemaMigrator.java（独立 @Component implements ApplicationRunner）
@Override
public void run(ApplicationArguments args) {
    try {
        Flyway.configure().dataSource(pgDataSource)
            .locations("classpath:db/pg/migration").baselineOnMigrate(true).load().migrate();
    } catch (Exception e) {
        log.error("Pg 迁移未执行（pg 不可达/配置缺失），检索将在运行时以 RETRIEVAL_FAILED 暴露", e);
    }
}
```

**教训**：多数据源 + 第二 Flyway 是代码审核高频雷区；前缀必须逐字对齐 yml，迁移触发用 `ApplicationRunner` 而非 `@PostConstruct`，并让第二数据源不可达时优雅降级，避免拖垮整个应用上下文。

**来源**：M5 T1 整改复验 2026-07-24

---

## 坑位 11：用 `@SpringBootTest` 测 controller 鉴权 401，会把整库上下文拉起来（headless 必败）（P0）

**现象（M5 T6 实测）**：`KnowledgeControllerAuthTest` 为 `@SpringBootTest` + `@AutoConfigureMockMvc`，只想验"未登录返回 401"，结果把整个应用上下文（主 MySQL Flyway/JPA/Hikari + 第二 Pg 数据源）全拉起；headless 无真实 DB，`flywayInitializer` 报 `jdbcUrl is required`，上下文启动失败，401 用例本身从未执行。导致该模块的 `mvn test` 无法全绿。

**真相（以实跑为准）**：验 controller 鉴权**只需要 Web + Security 过滤链**，根本不需要 DB 上下文。用 `@SpringBootTest` 测 401 属于"杀鸡用牛刀"，把整库上下文耦合进单测——即便业务逻辑用 `@MockBean` 了，上下文启动仍需真实 DB 才能 boot，既违反 §7.10 规则35"测试不依赖真实外部资源"的精神，也让"mvn test 全绿"在 headless 环境不可达。**这是测试选型错误，不是业务代码缺陷**。

**正确做法**：改 `@WebMvcTest(KnowledgeController.class)` 切片 + `@MockBean(KnowledgeService.class)` + 引入 Security 配置（或 `apply(springSecurity())`）。该切片只加载 Web 层，**不加载 DataSource/Flyway**，headless 可直接 green：
```java
@WebMvcTest(KnowledgeController.class)
@AutoConfigureMockMvc(addFilters = true)
class KnowledgeControllerAuthTest {
    @Autowired MockMvc mockMvc;
    @MockBean KnowledgeService knowledgeService;
    @Test void testController_noLogin_returns401() throws Exception {
        mockMvc.perform(post("/api/knowledge/upload")).andExpect(status().isUnauthorized());
    }
}
```

**排查命令**：
- `search_content` 在 `src/test` 搜 `@SpringBootTest`，逐一审视：这个测试**真的需要全上下文吗**？只验 controller/鉴权/HTTP 层的一律应换成 `@WebMvcTest` 切片。
- 跑 `@SpringBootTest` 测试若出现 `jdbcUrl is required` / `flywayInitializer` 失败，先判断是不是"只需 Web 层却用了全上下文"（本坑），而非直接去改 DB 配置（坑位10）。
- 区分：需要 JPA 派生查询 → `@DataJpaTest`（规则35 例外，坑位2）；只需 Web/鉴权 → `@WebMvcTest`；两者皆非则纯 Mockito。

**教训**：controller 鉴权/HTTP 单测用 `@WebMvcTest` 切片即可，**绝不用 `@SpringBootTest` 测 401**——后者会把整库上下文拉进来，headless 必败且违反规则35精神。

**来源**：M5 T6 深度验收 2026-07-24

---

## 坑位 13：编排类服务用 @Mock 全 mock 外部依赖，跨模块 import 内部类被"假合规"掩盖（P1 · test green ≠ 合规）

**现象（M6 T3 实测）**：`ConversationService` 把 `ModelService`/`AgentService`/`RetrievalPort`/`EmbeddingService`/`UserRepository` 全部 `@Mock`，于是即便它静态 `import` 了 `knowledge.service.EmbeddingService`（knowledge 内部 `@Service` 实现类）和 `user.UserRepository`（user 内部 repository），代码照样编译、单测全绿（12 tests, 0 failures）。但 §3.2 硬约束明确"禁止直接 import 其他模块的 entity/repository/impl 等内部类"，且 M6 计划 + 全局 gate 规定 conversation 仅依赖 `ModelService/AgentService/RetrievalPort` 三个发布接口。

**真相（以实跑为准）**：单测绿只证明"逻辑对"，证明不了"跨模块解耦合规"。mock 让对方内部类"存在"，import 越界被完全掩盖。**检索知识本应只依赖发布的 `RetrievalPort`（或 `KnowledgeService.retrieve(text,...)`），却绕过端口自己调 `EmbeddingService.embedSlices` 产出 embedding 再喂给 `retrievalPort.retrieve`——越界访问 knowledge 内部。** 用户解析同理，应走发布的 `UserService` 或在服务内 `currentUser()` 解析，而非 `user.UserRepository`。

**排查命令**：
- 审核编排类任务第一步就 `search_content` 目标包（如 `conversation`）的 `^import` 清单，逐条比对"是否只 @Autowired 计划约定的那几个发布接口"，再回头看测试是否被 mock 掩盖。
- 凡出现 `import ...knowledge.service.` / `import ...user.UserRepository` / `import ...agent.repository.` 等"他模块 service/repository/impl"即 §3.2 红色警报。

**教训**：单测绿 ≠ 跨模块解耦合规。编排类服务的 import 清单必须人工 grep 核对，mock 会把内部类依赖伪装成"存在"。

**来源**：M6 T3 代码审核 2026-07-24

---

## 坑位 14：控制器 `currentUser()` 把 principal 强转成 User，但 AuthFilter 只塞 username，被 @WebMvcTest 吞异常掩盖（P0 · test green ≠ 合规）

**现象（M6 T3 实测）**：`ConversationController.currentUser()` 写 `return (User) auth.getPrincipal();`，而 `common.security.AuthFilter` 把 principal 设成 `username`（String，`new UsernamePasswordAuthenticationToken(username, null, ...)`）。生产环境 `GET /api/chat`、`GET /api/chat/{id}/messages`、`DELETE /api/chat/{id}` 每次请求直接 `ClassCastException`（仅 `POST /api/chat/stream` 因在服务内用 `auth.getName()` 解析而幸免）。`ConversationControllerTest`（`@WebMvcTest`）里异常被 `GlobalExceptionHandler` 吞成 200，测试只断言 `status().isOk()` 且**未 `verify(conversationService.xxx())`** → test green 但接口挂。

**真相（以实跑为准）**：凡控制器取当前用户，必须确认 `SecurityContext` 中 principal 的**真实类型**（看 `AuthFilter` 怎么 `setAuthentication`）。`String` principal 不能强转 `User`。`stream()` 路径正确是因为服务内 `currentUser()` 用 `auth.getName()` 查库，控制器路径错误是因为直接 `(User)` 强转。

**正确做法**：
- 控制器不要向服务传 `User`；服务方法（`listConversations`/`listMessages`/`deleteConversation`）仿照 `stream()` 在内部用 `currentUser()` 解析（SecurityContext 取 username → userRepository 查）。或让 `AuthFilter` 注入 `User` 主体（影响面更大）。
- 测试必须 `verify(service.xxx())` 而不仅看状态码，否则阻断级 bug 会被异常处理器掩盖。

**排查命令**：
- `search_content` 在 `src/main` 搜 `getPrincipal\(\)\)` / `\(User\)\s*auth` / `getPrincipal\(\)\s+as`，确认没有把 String principal 强转成实体。
- 对照 `AuthFilter.doFilterInternal` 里 `setAuthentication` 的实参类型（是 username 还是 User）。

**教训**：控制器取当前用户必须核对 principal 真实类型；`@WebMvcTest` 断言要 `verify` 业务调用，不能只 `status().isOk()`——否则 `ClassCastException` 这类阻断 bug 会被全局异常处理器静默成 200。

**来源**：M6 T3 代码审核 2026-07-24

---

## 坑位 15：Git Bash/MinGW 下 mvn 报 ClassNotFoundException(classworlds launcher)，构建跑不起来（P2）

**现象（M6 T4 代码审核 2026-07-25 实测）**：在 Git Bash（MinGW）里直接 `mvn compile`/`mvn test` 报 `错误: 找不到或无法加载主类 org.codehaus.plexus.classworlds.launcher.Launcher` / `java.lang.ClassNotFoundException`。即便新下载解压 Maven 3.9.9，其 `boot/plexus-classworlds-*.jar` 确实存在且内含该 class，仍报同样错；原机 D: 盘 Maven 的 `lib` 还因缺 `classworlds-launcher.jar` 本身损坏。

**真相（以实跑为准）**：`mvn` 启动脚本在 MinGW 分支只把 `MAVEN_HOME` 转成 unix 风格 `/c/Users/...`（不像 cygwin 分支做 `cygpath --windows`），随后把 `-classpath "/c/Users/.../boot/plexus-classworlds-*.jar"` 交给 Windows 原生 `java.exe`；`java.exe` 不认 `/c/...` 这种 unix 路径，找不到 jar → ClassNotFoundException。这是**构建环境坑，不是代码问题**，但会直接卡死坑位6「必须实跑测试才能说通过」。

**排查/修复**：
- 用**原生 Windows shell**（PowerShell 或 cmd）跑 Maven，路径即变 Windows 风格，`java` 可解析：`& "C:\Users\admin\apache-maven-3.9.9\bin\mvn.cmd" test -Dtest=ConversationServiceTest`。在 PowerShell 工具里直跑即可（Git Bash 里的 `mvn` 不行）。
- 若 Maven 本身损坏：从 apache 镜像下 `apache-maven-3.9.x-bin.zip`（注意 Git Bash 下 `curl -o` 写 `/tmp` 或 `/c/Users/...` 会报"系统找不到指定的文件"，应写进工作区路径如 `F:/.../_temp/`；解压用 PowerShell `Expand-Archive` 比 Git Bash `unzip` 可靠，否则 `boot` jar 可能解坏导致同样的 ClassNotFoundException）。

**教训**：本仓库代码审核要"实跑测试"时，必须用原生 Windows shell（PowerShell）调用 `mvn.cmd`，别在 Git Bash 里跑 `mvn`；否则会卡在 ClassNotFoundException，误以为代码编译失败、或被迫"没跑就报通过"（踩坑位6 红线）。

**来源**：M6 T4 代码审核 2026-07-25

---

## 坑位 16：前端 vite build 被 WorkBuddy safe-delete 拦截 trash dist 而误报"Build failed"（非代码错误）（P2）

**现象**：`npm run build`（vite）在日志末尾报 `[safe-delete] 操作失败: ERROR .../dist/assets: Error during a `trash` operation: Unknown { description: "Some operations were aborted" }`，构建中断；但同一条日志里 `✓ NNNN modules transformed.` 已经成功输出。

**真相（以实跑为准）**：Vite 在写出产物前会 `emptyDir(outDir)` 清空旧 `dist`；WorkBuddy CLI 的 `genie-safe-delete` 拦截 `fs.rmSync` 改走"回收站 trash"，而该 trash 操作在本环境失败抛错，导致构建在**收尾清理阶段**中断。前端代码本身 0 错误（全部模块 transform 成功）。这是**环境坑、非代码缺陷**，但会卡死"实跑构建"这条验收红线（类比坑位15 对 maven 的影响）。

**排查/修复**：用**全新输出目录**绕过清理：`npx vite build --outDir <不存在的目录>`（目录不存在则 `emptyDir` 不触发 trash，构建正常产出）；或本地非 WorkBuddy 终端直接 `npm run build`。结论必须基于真实构建输出，别因这条环境报错误判"前端编译失败"。

**教训**：本仓库前端模块（F1–F6 / M7 mcp 页）审核"实跑构建"时，若 `npm run build` 卡在 safe-delete trash，改用 `--outDir` 新目录验证；`✓ modules transformed` + `✓ built` + exit 0 才是真通过信号。

**来源**：M6 T5/F5 代码审核 2026-07-25

---

## 坑位 21：docker-compose 相对路径以 compose 文件所在目录为基准，`config` 不校验宿主路径存在性（P0）

**现象（M7 T5 复审实测）**：`deploy/docker-compose.yml` 按"从仓库根执行"的直觉写 `context: .`、`./deploy/nginx.conf`、`./frontend/dist`、`./deploy/initdb`。实跑 `docker compose -f deploy/docker-compose.yml config` 发现全部解析为 `deploy\deploy\nginx.conf`、`deploy\frontend\dist` 等**不存在的路径**，build context 也错为 `deploy/`（Dockerfile `COPY backend/target/*.jar` 必失败）。更隐蔽的是：`docker compose config` **exit 0 且不校验宿主路径是否存在**，T4/T5 首轮审核均以"config 通过"为证据而漏过；`up` 时 Docker 会把不存在的 bind 源当空目录静默创建 → nginx SSE 配置静默丢失（与坑位11"静默失效"同模式）。

**真相（以实跑为准）**：compose 的相对路径（volumes bind 源、build context、env_file 等）一律以 **compose 文件所在目录**为基准解析，与执行 `docker compose -f` 时的 cwd 无关。`config` 只做插值/结构校验，不做宿主路径存在性校验。

**排查命令**：
- `docker compose -f <file> config | grep -E "source:|context:"`，逐条核对解析出的**绝对路径**是否真实存在（`ls` 逐个验证），别只看 exit 0。
- 凡 compose 文件不在仓库根：检查文件内是否出现 `./<compose所在目录名>/...` 形态的自嵌套路径（如 `deploy/` 下写 `./deploy/xxx`），即为错位信号。

**修复样板**（compose 在 `deploy/` 下）：`context: ..` + `dockerfile: deploy/Dockerfile`；同目录文件 `./nginx.conf`；仓库根下文件 `../frontend/dist`。

**教训**：审核 compose 部署物料时，必须实跑 `config` 并逐条核对解析后的宿主路径**存在性**；`config` exit 0 ≠ 路径正确——bind 源错位是静默失效（Docker 自动建空目录），比启动报错更危险。

**来源**：M7/T5 二次深度复审 2026-07-26

---

## 坑位 22：死 `catch (Exception e)` 包裹根本不抛受检异常的代码（如 `new String(charset)`），不可达且违反 §7.3 规则11（P2）

**现象（K3 代码审核 2026-08-04 实测）**：`TextParser.parse` / `MarkdownParser.parse` 把 `new String(content, StandardCharsets.UTF_8)` 包在 `try { ... } catch (Exception e) { throw new BizException(FORMAT_CORRUPTED, ...); }` 里。但 `String(byte[], Charset)` 构造器**不声明任何受检异常**——UTF-8 是内置必有 charset，字节非法只会静默替换成 `\uFFFD` 而非抛错——这个 catch 分支**永远不可达**，是死代码；同时 `catch (Exception e)` 一把抓本身也违反 §7.3 规则11。

**真相（以实跑/通读为准）**：§7.3 规则11「捕获具体异常，不写 `catch (Exception e)` 一把抓」。当 try 块内代码根本不可能抛异常时，这个 try/catch 既无保护意义、又违反规则11、还误导维护者以为这里会出解码异常。`new String(bytes, charset)` 只有在用运行时 `Charset.forName("xxx")`（受检 `UnsupportedEncodingException`）时才可能抛，用常量 `StandardCharsets.UTF_8` 时绝不会。

**排查命令**：`search_content` `catch\s*\(\s*Exception` 命中后，逐一审视 try 块——若块内只有 `new String(...)` / 简单字段访问 / 纯计算等不抛受检异常的语句，该 catch 即死代码，直接删掉 try/catch、保留原语句即可。

**正确做法**：删除多余的 try/catch，直接 `String text = new String(content, StandardCharsets.UTF_8);`；真正可能抛 `IOException`/`TikaException` 的边界（如 `tika.parseToString`）才 `catch (IOException | TikaException e)` 转 `BizException`。

**教训**：`catch (Exception e)` 不只在「内层已转 BizException」时是死分支（坑位18）；**包在不抛异常的代码外更是赤裸裸的不可达死代码**，两者都违反 §7.3 规则11，审核时都要 grep 出来删掉。

**来源**：K3 文档解析器深度验收 2026-08-04

---

## 坑位 23：注册型常量（ErrorCode/枚举）定义但零引用 = 死代码，"新增 N 项"验收点假达标（P2）

**现象（K3 代码审核 2026-08-04 实测）**：K3 计划验收点5 要求「ErrorCode 新增 4 项解析失败码」。`ErrorCode` 确实加了 `DOC_PARSE_FAILED(5006)`/`ENCRYPTED_PDF(5007)`/`SCANNED_PDF_NO_TEXT(5008)`/`FORMAT_CORRUPTED(5009)`，4 项齐备，首轮验收据此判 ✅。但 `search_content` 全仓搜 `DOC_PARSE_FAILED` 仅命中定义处——**没有任何解析器抛出它**，纯死代码。配套地，`DocumentChunker` 还有 `private static final int FALLBACK_OVERLAP = 120;` 定义后从未被引用。两者都是「声明处有、消费处无」的死代码。

**真相（以实跑为准）**：计划说「新增 N 项」只证明"登记了"，证明不了"被使用了"。ErrorCode 是 API 响应契约（§3.5），定义不抛 = 契约里挂着一块永远用不上的牌子；常量同理。「新增 N 项」验收点被假达标——看一眼枚举就算过，实际死代码。这与坑位11「配置/列已定义但未接入业务链路」同源，但发生在**注册型常量（ErrorCode/枚举）**这一更隐蔽的物种上。

**排查命令**：对每个"新增的 ErrorCode/枚举/常量"，`search_content` 其名字，确认除定义外**至少有 1 处真实引用**（抛出处/消费点）；0 引用即死代码。对 `private static final` 常量同样 grep 名字确认被读取。

**正确做法**：定义即消费——给 `DOC_PARSE_FAILED` 接上真实抛出处（PDF/DOCX 解析器 `tika.parseToString` 抛 `IOException|TikaException` 的通用分支转 `DOC_PARSE_FAILED`，与 `ENCRYPTED_PDF`/`SCANNED_PDF_NO_TEXT`/`FORMAT_CORRUPTED` 细分互补）；定义但确实无场景的常量直接删掉，别为了"凑 N 项"留死代码。

**教训**：「新增 N 项」类验收点必须 grep 确认每项都有真实引用，定义 ≠ 达标；注册型常量死代码与配置死代码（坑位11）同害，审核时一律反向追消费点。

**来源**：K3 文档解析器深度验收 2026-08-04

---

## 坑位 24：自定义构建/验收脚本的 `rm -rf` 被 Bash 沙箱 safe-delete 拦截，阻断「实跑」结论产出（P2）

**现象（K3 代码审核 2026-08-04 实测）**：沿用 K1/K2 的 `k3_test.sh` 跑单测，脚本开头 `rm -rf "$MAIN_OUT" "$TEST_OUT" "$K3RES"` 清理旧产物。在本环境 Bash 工具下直接被 `[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED]` 拦截（172 个 target 触发批量删除确认），脚本在第一步就中止，**后续的 javac/实跑从未执行**——首轮后台跑出来的只有一条 safe-delete 拦截日志，没有任何 `Tests run` 输出。若没察觉、把"脚本跑完了"当"测试通过了"，就直接踩坑位6 红线（没实跑报通过）。

**真相（以实跑为准）**：WorkBuddy Bash 沙箱对 `rm -rf` 批量删除有安全拦截（尤其 target 数 ≥ 阈值）。这与坑位16（genie-safe-delete 拦截 vite `fs.rmSync` 清 dist）是同一类"safe-delete 阻断收尾"环境坑，但触发点是 **Bash 工具里的 `rm -rf` 命令**而非前端构建。它不会报"测试失败"，而是让"实跑"根本不发生，比报错更隐蔽。

**排查/修复**：
- 改每次跑用**独立输出目录**（如 `MAIN_OUT="$K3/run_$$/main"`），天然干净重建，**彻底不调用 `rm -rf`**，绕过拦截。
- 若必须清理：`dangerouslyDisableSandbox: true` 跑该命令（仅在你自己的临时构建目录、无用户个人文件时）；`find ... -delete` 仍可能触发，优先用独立目录法。
- 凡用自定义 `*.sh` 做"实跑验证"，跑完**必须看 stdout 里有没有真实的 `Tests run / Failures` 数字**——只有 safe-delete 日志说明脚本被拦在半路，结论不能成立。

**教训**：自定义验收/构建脚本避开 `rm -rf`；用独立输出目录做干净重建。判断"实跑成功"的唯一标准是**真实测试输出**，不是"命令退出了"。

**来源**：K3 文档解析器深度验收 2026-08-04

---

## 坑位 25：跨库两段式软删下推——IN 过滤的 id 类型与 PG chunk 表列口径不一致，混合检索零命中（P0）

**现象（K4 混合检索深度验收 2026-08-04 实测）**：K4 跨库软删两段式·第一段在 MySQL 用 `SELECT d.id FROM Document d JOIN KnowledgeBase kb ...` 筛出"未软删"的有效文档，再把这些 id 下推到 PG `WHERE document_chunk.document_id IN (:validDocIds)` 做过滤。单测 13/13 绿、原验收报告据此判"P1 跨库软删生效（删了的文档搜不到）"。但 `document_chunk.document_id` 列是 `VARCHAR(64)`，存的是 `Document.documentId`（对外暴露的 UUID **字符串**，由 `DocumentChunkRepository.saveChunk(String documentId, ...)` 写入）；而 `findIdsByKbIdIn` 返回的是 `Document.id`（自增 **Long 主键**）。Long 主键推进去与 chunk 表里的 UUID 字符串对不上 → 混合检索一条都搜不出来（或 `varchar::integer` 转型失败抛 RETRIEVAL_FAILED）。

**真相（以实跑/通读为准）**：跨库两段式"先 MySQL 取有效 id → 下推 PG IN"的关键是**两段 id 必须同口径**。MySQL 侧取 id 的列必须选 chunk 表那列实际存的值：`document_chunk.document_id` 存业务 UUID，故 `findIdsByKbIdIn` 必须 `SELECT d.documentId`（String），绝不能 `SELECT d.id`（Long 主键）。同理任何"跨库用一份 id 列表互相 IN 过滤"的场景，都要先核对两库该列的真实类型/取值口径，不能想当然用 JPA 主键。单测用 `when(repo.findIdsByKbIdIn(...)).thenReturn(List.of(11L, 12L))` 同类型 stub，再断言"下推的就是这串 Long"——纯属自己骗自己，完全掩盖了类型错配（典型 坑位1/11 "test green ≠ 合规"）。

**排查命令**：
- 凡出现"MySQL 取 id 列表 → 下推 PG `IN (:ids)`"模式：`search_content` 确认取的是 `d.documentId`（业务 id）还是 `d.id`（主键）；再比对 chunk 表迁移里该列类型（`grep -n "document_id" db/pg/migration/*.sql`）与索引器写入该列的值（`grep -n "saveChunk\|INSERT INTO document_chunk"` 看传的是 `document.getId()` 还是 `document.getDocumentId()`）。
- 单测若 stub 了取 id 的 repo，断言里改用**与 chunk 表同口径的业务 id 字符串**，而非 Long 主键，避免自我掩盖。

**正确做法**：`findIdsByKbIdIn` 返回 `List<String>`（`SELECT d.documentId`）；端口内 `validDocIds` 用 `List<String>`；下推 `IN (:validDocIds)` 与 `document_chunk.document_id`（varchar）口径一致。单测回归用 `List.of("doc1","doc2")` 业务 id，断言下推列表等于这些业务 id。

**教训**：跨库用 id 列表互相 IN 过滤，两段 id 必须同口径（业务 id vs 主键 Long 绝不能混）；单测 stub 取 id 的 repo 时要造"与对端表列同口径"的值，否则"绿"只是同类型自我印证。

来源：K4 混合检索 R2 + 跨库软删深度验收 2026-08-04

---

## 坑位 26：异步编排器顶层 catch(Exception) 是必需兜底 + 复制粘贴致悬空/重复 Javadoc（P3）

**现象（K6 异步索引流水线深度验收 2026-08-05 实测）**：
1. `IndexingJobService.process`（异步火忘入口，`parseExecutor.execute(() -> process(jobId))`）用 `catch (Exception e)` 兜底记录 FAIL。初看像违反 §7.3 规则11「不写 catch (Exception e) 一把抓」；但它是异步链的**唯一出口**——没有调用方接异常，必须捕获并落库（不静默吞、记 failStage/errorCode/errorMessage + 日志），属**必需的异步兜底**，并非死代码（与坑位18「内层已转 BizException 后外层 catch(Exception) 成死分支」不同：此处 catch 真正干活）。
2. 同文件 `readChunks` / `chunksPayload` 的 `MAPPER.readValue` / `writeValueAsString` 用 `catch (Exception e)`，而 Jackson 仅抛 `IOException`（受检，extends IOException）——与坑位18/22 同源，应收窄到 `IOException`。
3. `IndexingJobRepository.findByBatchIdAndStatus` 上方因复制粘贴出现**悬空/重复 Javadoc 块**：多一个 `/** 批量一键重试…` 开头 + 空行 + 内嵌第二 `/**`，虽能编译（注释不嵌套，整段被当一块注释），但注释脏乱、易误导。

**真相（以实跑/通读为准）**：
- 异步编排器（ConversationService 流式、MCP 调用、索引流水线）顶层 `catch (Exception e)` 是为"无调用方接异常"而存在的兜底，收窄到 `catch (RuntimeException e)` 即可（此类方法体只抛 RuntimeException，等价且更贴合规则11）；**不要把它当成规则11 违规一概删除**——删了异常就丢失在 executor 线程里。判据：看该 catch 是否"真正干活"（记 FAIL/落库/降级），干了就是合理兜底，没干（死分支）才是坑位18。
- `ObjectMapper` 序列化辅助的 `catch (Exception e)` 一律收窄到 `IOException`（坑位18/22），并补 `log.warn("…", e)` 满足 §7.3 规则10「必须处理/记录」。
- 复制粘贴 Javadoc/注释块要逐段核对，避免悬空开头与内嵌 `/**`；虽不阻断编译，但属代码整洁度缺陷（P3）。

**排查命令**：
- `search_content` `catch\s*\(\s*Exception` 命中后按"是否异步顶层兜底 / 是否 Jackson 序列化 / 是否远程边界死分支"分类处置（对应坑位18/22/26）。
- `search_content` 各 repository 接口的 Javadoc 注释块，查是否有重复 `/**` 开头或内嵌 `/**`。

**教训**：异步编排器顶层 catch 是合法兜底，勿误判规则11；JSON 序列化 catch 仍须收窄 IOException；复制粘贴注释要复查。三者都靠"实读代码 + 分类判断"而非"见 Exception 就报"。

**来源**：K6 异步索引流水线深度验收 2026-08-05

---

## 坑位 27：单测 classpath 回补了部署产物缺失的传递依赖，测试绿掩盖线上故障（P1）

**现象（K0808 T4/T5 代码审核 2026-08-08 实测）**：T4 新增 `TikaDocParser`（解析老 `.doc`，底层走 Tika + POI HWPF）。单测 `TikaDocParserTest.testParse_validDoc_returnsText` 从真实 `.doc` 抽出「WPS」文本、27/27 全绿。但部署产物（`fat jar` / 展开运行实例 `F:/hify_tmp/BOOT-INF/lib`）实际**不含 `poi-scratchpad`**（仅 `poi-5.2.5.jar` + `tika-parser-*`），`HWPFDocument` 类不存在。即：单测绿只因 verify 脚本从 `.m2` 临时补了 `poi-scratchpad-5.2.5.jar` 到 classpath，而**真实 fat jar 缺这个传递依赖**——`.doc` 上传在线上会因 `ClassNotFoundException(HWPFDocument)` 失败。代码正确 + 测试绿，但功能线上不可用。

**真相（以实跑/通读为准）**：本环境**无 Maven**，fat jar 是早先用 `mvn package` 打出来的老包；Tika 的 `tika-parser-microsoft-module` 把 `poi-scratchpad` 作为**传递依赖**，只有重新 `mvn package` 才会把它打进 fat jar——老包（以及 T3 时期展开的 `F:/hify_tmp`）都没有。验收脚本为让测试可跑，从 `.m2` 补 jar，反而把「部署产物缺依赖」这个真问题盖住了。这与坑位1/坑位6「test green ≠ 合规」同源，但是更隐蔽的一种：**测试环境的 classpath 比部署产物更全**，绿的是「回补后的测试态」而非「线上态」。

**排查命令**：
- 凡解析器/客户端依赖 **传递性第三方库**（特别是 Tika/POI/HWPF、OkHttp、Jackson 数据格式扩展等），实查部署产物：`ls <fatjar>/BOOT-INF/lib | grep -iE "scratchpad|poi-|tika-"` 确认传递依赖在列；`unzip -l <jar> | grep -i <关键类>` 验证类真在内。
- 看 verify/accept 脚本是否从 `.m2` 额外 `SCRATCH=...poi-scratchpad...jar` 之类补 jar；**若有，该 jar 必须同步进部署产物**，否则测试绿 ≠ 线上可用。
- 对「无 Maven 环境」，展开运行实例后务必把新类 + 缺失传递依赖一起补进 `BOOT-INF/{classes,lib}`，否则线上既缺代码又缺依赖。

**教训**：无 Maven 环境 fat jar 可能缺传递依赖；**单测若要靠从 `.m2` 补 jar 才绿，必须同步补到部署产物**，否则 test green ≠ 线上可用——这是「测试绿掩盖生产故障」的高危变种，验收时反向核对部署产物 lib 而非只看测试 classpath。

**来源**：K0808 T4/T5 代码审核 2026-08-08

---

## 坑位 28：无 Maven 环境手工注入部分 K0808 类到运行实例，混合新旧类状态掩盖其他未注入类的回归（P2）

**现象（K0808 T6 端到端验收 2026-08-08 实测）**：T6 端到端验证在运行实例 `F:/hify_tmp` 上实跑，`.doc` 上传→索引→检索全绿、坑位27（poi-scratchpad）确认闭合。但核查关键类时间戳发现：实例 `BOOT-INF/classes` 含 T4 新注入的 `TikaDocParser.class`/`DocType.class`（Aug 8），而 `DocumentChunker.class` 时间戳是 **Aug 7 18:11（T1 小数点修复之前的旧版）**。实例处于「混合新旧类」状态——只注入了部分 K0808 类，未随 K0808 全量重建。T6 验收点2 期望「overlap=120」，实跑却发现 `.doc` 相邻块无重叠；根因正是运行实例的 `DocumentChunker` 是旧版、且 overlap 由 `RagConfig.chunkOverlap()` 配置驱动——若只看 `.doc` e2e 全绿，T1 的修复是否真在线上生效根本无从验证（旧类把回归盖住了）。

**真相（以实跑/通读为准）**：本环境无 Maven，运行实例是「展开 fat jar 的 classes + lib」手动 `-cp` 启动。K0808 各 T 任务改动散落多文件（T1 DocumentChunker、T2 QueryIntentClassifier、T3 RagQueryService、T4 TikaDocParser…），若只把「当前在测的 T」的类注入实例、不重建全量，实例就会混杂多个版本的 class。e2e 在某文件上绿，不代表其他已改文件（如 T1）的修复也生效——**绿的是「部分注入后的实例态」，不是「K0808 全量合入态」**。这与坑位12（增量编译 stale class）同源，但发生在「运行实例」层面而非「本地编译」层面；也接续坑位27（缺传递依赖）的「实例态 ≠ 部署产物态」主题。

**排查命令**：
- e2e 验收前，对本次 K0808 改动涉及的全部主类，批量核对运行实例 `BOOT-INF/classes` 的 `.class` 时间戳是否 ≥ 源码最后修改时间：`ls -la F:/hify_tmp/BOOT-INF/classes/com/hify/hify/knowledge/chunk/DocumentChunker.class F:/hify_tmp/BOOT-INF/classes/com/hify/hify/knowledge/service/QueryIntentClassifier.class ...`
- 任一关键类时间戳早于其源码最新改动 → 实例为混合态，e2e 结论对该文件不可信，须先把该类也重编注入（或全量重建实例）再验。
- 对「配置驱动」行为（如 overlap 取 `RagConfig.chunkOverlap()`），e2e 比对前先 `grep` 源码确认期望值来源，别把「配置未设」误判成「代码未生效」。

**教训**：无 Maven 环境做 e2e 验收，手工注入类须「全量或显式核对」——只注入待测 T 的类会造出混合新旧类实例，让其他已改文件的回归被旧类掩盖（e2e 假绿）。验收前批量核对关键类 `.class` 时间戳 ≥ 源码改动时间；K0808 全量收尾后应统一重建运行实例，纳入 T1–T5 全部类 + 缺失传递依赖。

**来源**：K0808 T6 端到端验收 2026-08-08

---

## 坑位 29：手工注入含嵌套内部类（枚举/record）的源文件漏拷独立 .class → 启动 ClassNotFoundException（P0）

**现象（K0808 T7 代码审核 2026-08-08 实测）**：T7 新增 `MessageFeedback.java`（含嵌套枚举 `Rating`）。按无 Maven 环境惯例手工把 `MessageFeedback.class` 拷进运行实例 `F:/hify_tmp/BOOT-INF/classes` 后重启，应用**上下文刷新直接失败**：`Caused by: java.lang.ClassNotFoundException: com.hify.hify.conversation.entity.MessageFeedback$Rating`；`MessageFeedbackRepository` 因引用 `MessageFeedback.Rating` 类型无法注册，整个 Spring 容器起不来。

**真相（以实跑为准）**：javac 把嵌套 enum/record/内部类编译成**独立类文件**（`MessageFeedback$Rating.class`、`Xxx$Yyy.class`）。手工拷文件时只拷了顶层 `MessageFeedback.class`，漏了 `MessageFeedback$Rating.class`；JVM 在解析 `MessageFeedback.Rating` 这个类型时找不到对应 .class → `ClassNotFoundException`。这是坑位28「手工注入类未完整重建实例」的更 Severe 变体：坑位28 是新旧混合导致**静默回归（e2e 假绿）**，本坑是缺内部类导致**硬启动失败**，比静默失效更直白但同样源于「只拷顶层类」。

**排查/修复**：
- `ls` 实例实体目录，确认嵌套类是否齐全：`ls F:/hify_tmp/BOOT-INF/classes/.../entity/ | grep -i "MessageFeedback"` —— 若只有 `MessageFeedback.class` 没有 `MessageFeedback$Rating.class`，即命中。
- **根治做法**：凡源文件含内部 enum/record/类，用 `javac -parameters -cp "<classes>;<lib/*>" -d <classes> *.java` 直接编译输出到目标 classpath（javac 会**同时产出顶层+全部 `$*.class`**），或手工拷贝时**整组**带上 `$*` 文件；**禁止只 cp 顶层 .class**。
- 重启后看日志 `grep -c ClassNotFoundException <run.log>` 应为 0，且仓储 bean 能注册。

**教训**：无 Maven 手工注入类时，含嵌套内部类的源文件必须把它们编译产物（顶层 + 全部 `$Xxx.class`）整组注入；单拷顶层类 = 埋一颗 `ClassNotFoundException` 的启动雷。

**来源**：K0808 T7 代码审核 2026-08-08

---

## 坑位 30：native query 传枚举参数默认按 ordinal 绑定，违反 §7.2（须传 enum.name() 字符串）（P1）

**现象（K0808 T7 代码审核 2026-08-08 实测）**：`MessageFeedbackRepository.upsert` 的 native SQL 把 `:rating` 直接声明为 `@Param("rating") MessageFeedback.Rating rating`（枚举）。若按此实现，`rating` 列（`varchar(16)`，设计为存 `'THUMBS_UP'/'THUMBS_DOWN'`）会落库成 `'0'/'1'`，直接违反 §7.2「禁魔法数字、存枚举字符串」。

**真相（以实跑/通读为准）**：Hibernate 对 **native query** 的枚举参数**默认按 ordinal(0/1) 绑定**（除非配置 `hibernate.query.sql.enum_type=STRING` 之类显式强制）；而 `@Enumerated(EnumType.STRING)` 只作用于实体字段映射，**不作用于 native query 的参数绑定**。本项目 `application.yml` 无任何该类配置。全仓既有 native query（`KnowledgeBaseRepository.updateRagConfig(String, Long)`）均只传 `String`/`Long`，T7 传枚举属偏离项目惯例。

**正确做法**：native query 的枚举参数改为传 `String`，由调用方 `rating.name()`（`THUMBS_UP`）；SQL 直接 `VALUES (... :rating ...)` 绑定字符串即正确落库。实体字段仍保留 `@Enumerated(EnumType.STRING)`（JPA 实体映射用），但**仓储 native 方法签名收 String**。T8 的 `MessageFeedbackService` 在把 DTO 的 `"THUMBS_DOWN"` 转成枚举后，调 `upsert(..., rating.name(), ...)` 即可，与 JSON 入参天然对齐。

**排查命令**：`search_content` 全仓 `@Query(... nativeQuery = true` 的方法，逐一审视其参数类型——凡出现 `MessageFeedback.Rating`/`SomeEnum` 等枚举入参即命中，改为传 `enum.name()` 字符串。

**教训**：native query 的枚举参数 ≠ 实体字段映射，Hibernate 默认 ordinal 绑定会把 `THUMBS_UP` 存成 `'0'`，既违反 §7.2 又使按字符串值（`WHERE rating='THUMBS_DOWN'`）的聚合查询全部失效；native query 一律传 `enum.name()` 字符串，契合本项目既有惯例。

**来源**：K0808 T7 代码审核 2026-08-08

---

## 坑位 31：controller 用 standaloneSetup 测 HTTP 时不加载安全链，@PreAuthorize 角色闸门不生效也不被测试，RBAC 须下沉 service 层抛 FORBIDDEN（P1）

**现象（K0808 T8 代码审核 2026-08-08 实测）**：`MessageFeedbackController` 的 `/api/chat/feedback`（管理员视角：被踩 TOP-N + 原因分布）需要 ADMIN 角色。但控制器测试 `MessageFeedbackApiTest` 用 `MockMvcBuilders.standaloneSetup(new MessageFeedbackController(service))`（坑位17：避开缺 `spring-security-test` 导致的 `@WithMockUser` 编译错）。`standaloneSetup` **只装配指定的 controller + 其直接依赖，不加载 `SecurityConfig` / 过滤器链 / 方法安全 AOP**，因此若此处用 `@PreAuthorize("hasRole('ADMIN')")` 做角色门控，在 standaloneSetup 下：① 不被强制（无 SecurityFilterChain 拦截）；② 测试里无从断言「非 ADMIN 被拒」（只能验路由委派）。T8 选择把角色校验从方法注解**下沉到服务层** `adminView` 内显式 `throw new BizException(ErrorCode.FORBIDDEN)`（用 `auth.getAuthorities()` 判 `ROLE_ADMIN`），并被 `adminView_nonAdmin_throwsForbidden` 单测覆盖。代码注释原本写「坑位 C2」——该编号并不存在（初次提交时占位未填真实号），属 坑位4 式「注释引用不存在的符号」缺陷，已修为「坑位 31」。

**真相（以实跑/通读为准）**：`standaloneSetup` 与 `@WebMvcTest` 不同——它不引入任何 `@Configuration`（含 `SecurityConfig`），所以 Spring Security 的方法级安全注解（`@PreAuthorize`/`@Secured`/`@RolesAllowed`，靠 AOP 拦截、需 `SecurityFilterChain` + `MethodSecurity` 配置）**完全不生效**。本仓库 controller HTTP 单测一律 standaloneSetup（坑位17），故**凡是需要「角色 / 权限」门控的端点，绝不能依赖注解式方法安全**——否则在测试里是「假门控」（绿但生产若没配 MethodSecurity 也不拦），且毫无断言覆盖。正确拆法：
1. **未登录 401**：由全局共享 `SecurityConfig` 过滤链统一拦截（生产生效；`ConversationControllerTest` 已覆盖 `/api/chat/**` 的 401）。
2. **角色 / 权限（如 ADMIN）**：在 service 方法内用 `SecurityContextHolder.getContext().getAuthentication().getAuthorities()` 显式判角色并 `throw BizException(ErrorCode.FORBIDDEN)`，以便 Mockito 单测直接覆盖（传入 `ROLE_USER` 断言抛 `FORBIDDEN`、传入 `ROLE_ADMIN` 断言放行）。控制器只负责把 `auth` 从 `SecurityContext` 传给 service（坑位14：principal 是 String username，用 `auth.getName()`，勿强转实体）。

**排查命令**：
- 凡控制器有角色要求：`search_content` 其 `@PreAuthorize|@Secured|@RolesAllowed`，若命中且同模块控制器测试是 `standaloneSetup`，该注解在测试中是「假门控」——确认对应 service 内是否做了 `FORBIDDEN` 兜底（`grep -n "ErrorCode.FORBIDDEN" <XxxService>.java`）。
- 反向：service 里抛 `FORBIDDEN` 的角色点，确认有单测断言（如 `assertThrows(BizException.class, ...)` 且 `ex.getErrorCode() == FORBIDDEN`），而非仅靠 `@PreAuthorize` 在测试里「看起来能拦」。
- 前端 `request` 拦截器对 `code≠0` 统一 `ElMessage.error`——`FORBIDDEN` 会被转成用户可见提示，控制器无需重复处理。

**教训**：本仓库 controller HTTP 单测用 `standaloneSetup`（坑位17）→ 方法安全注解不加载 → 角色门控必须下沉 service 层抛 `FORBIDDEN`，勿误以为「加个 `@PreAuthorize` 就安全了」。这是与坑位17（缺 `@WithMockUser` 依赖）、坑位14（principal 是 String 用 `auth.getName()`）、坑位4（注释别引用不存在的符号）配套的「controller 测试四件套」。

**来源**：K0808 T8 代码审核 2026-08-08

---

## 坑位 32：Python 工具脚本用 `%` 格式化拼接 HTML 时，CSS 里的 `100%` 被当格式化符 → ValueError: unsupported format character（P2 · Python 工具脚本专属）

**现象（K0808 T12/T13 代码审核 2026-08-08 实测）**：`tools/kb_health_report.py` 生成离线自包含 HTML 报告时，最初用 `"...width:100%%;...%s..." % (...)` 这类 `%` 运算符拼 HTML 模板。一旦 CSS 片段里出现字面量 `%`（如 `width:100%`、`margin:0%`、`font-size:90%`、`opacity:50%`），Python 会把 `100%` 后的字符当格式化指令解析，抛出 `ValueError: unsupported format character ';'`（或 `';' not a valid conversion`）。这是 **Python 字符串格式化** 的专属坑，与 Java 无关，但凡本仓库 `tools/*.py` 里「用 `%` 拼带 CSS/样式的 HTML」都会踩。

**真相（以实跑为准）**：自包含 HTML 报告（T13 MUST：SVG 内联、CSS 内联、零 CDN）几乎必然含大量 `%` 字面量。两种可行修法：
1. **把整段 CSS 抽成静态字符串常量**（如本脚本的 `REPORT_CSS`），不参与任何 `%` 格式化，只在最后 `head = "..." + REPORT_CSS + "..."` 用字符串拼接——CSS 里随便写 `%` 都不会被解析。这是最稳的写法。
2. 若坚持用 `%`：CSS 里的 `%` 必须转义成 `%%`（每处都要改，易漏），且不能与 `%s/%d` 混排出错，维护成本高，**不推荐**。
3. 或改用 `.format()` / f-string / `str.replace("{css}", REPORT_CSS)` 占位，彻底避开 `%` 解析。

**排查命令**：
- `grep -nE "%\s*\(|%.*%" tools/*.py` 找「用 `%` 运算符拼 HTML/CSS」的脚本；再 `grep -n "100%\|width:\|margin:\|opacity:"` 看模板里是否含未转义 `%`。
- 实跑验证：直接 `python tools/xxx.py --from-json <已有json> --report-out /tmp/x.html`（多数报告脚本支持只读重生成，跳过网络），若报 `unsupported format character` 即命中本坑。

**教训**：本仓库 `tools/` 下的 Python 可视化/报告脚本（如 `kb_health_report.py`、`view_chunks.py` 衍生件）凡涉及生成 HTML，一律 **CSS 抽静态常量 + 字符串拼接**，禁止用 `%` 运算符格式化含 CSS 的模板。这是与坑位1/坑位6「实跑才知真假」同一思路在 Python 工具层的延伸——不真跑生成，永远发现不了这个 ValueError。同时记一条复用约束：离线报告图表必须用真 `<svg><rect>/<polyline>` 渲染、零 CDN（T13 MUST），不能用 CSS div 模拟图表。

**来源**：K0808 T12/T13 代码审核 2026-08-08
